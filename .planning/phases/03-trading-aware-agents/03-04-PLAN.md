---
phase: 03-trading-aware-agents
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - topstepx/agents/tsx-phase-researcher.md
  - topstepx/agents/tsx-research-synthesizer.md
  - topstepx/agents/tsx-integration-checker.md
  - topstepx/agents/tsx-nyquist-auditor.md
autonomous: true
requirements: [AGT-09, AGT-10, AGT-11, AGT-12]

must_haves:
  truths:
    - "tsx-phase-researcher agent exists with trading domain research context"
    - "tsx-research-synthesizer agent exists with trading research synthesis awareness"
    - "tsx-integration-checker agent exists with trading API wiring verification (auth -> orders -> positions -> WebSocket)"
    - "tsx-nyquist-auditor agent exists with trading test pattern awareness"
    - "All four agents have zero GSD naming remnants"
    - "All four agents preserve GSD structural patterns"
  artifacts:
    - path: "topstepx/agents/tsx-phase-researcher.md"
      provides: "Trading-aware phase research agent"
      contains: "name: tsx-phase-researcher"
    - path: "topstepx/agents/tsx-research-synthesizer.md"
      provides: "Trading-aware research synthesis agent"
      contains: "name: tsx-research-synthesizer"
    - path: "topstepx/agents/tsx-integration-checker.md"
      provides: "Trading-aware integration verification agent"
      contains: "name: tsx-integration-checker"
    - path: "topstepx/agents/tsx-nyquist-auditor.md"
      provides: "Trading-aware test coverage auditor"
      contains: "name: tsx-nyquist-auditor"
  key_links:
    - from: "topstepx/agents/tsx-integration-checker.md"
      to: "$HOME/.claude/topstepx/references/TOPSTEPX_API.md"
      via: "trading integration wiring verification"
      pattern: "auth.*order\|WebSocket\|API.*wiring"
    - from: "topstepx/agents/tsx-phase-researcher.md"
      to: "$HOME/.claude/topstepx/references/"
      via: "reference loading for phase research"
      pattern: "topstepx/references"
---

<objective>
Adapt the four smallest supporting GSD agents (gsd-phase-researcher at 555 lines, gsd-research-synthesizer at 249 lines, gsd-integration-checker at 445 lines, gsd-nyquist-auditor at 178 lines) into TSX equivalents. This completes the full roster of 12 trading-aware agents.

Purpose: These agents handle phase-level research, research synthesis, cross-phase integration verification, and test coverage auditing. The integration-checker needs meaningful trading additions (API wiring verification). The others need primarily naming changes with lighter trading context.

Output: Four agent definition files in topstepx/agents/ completing the full set of 12 agents.
</objective>

<execution_context>
@$HOME/.claude/topstepx/workflows/execute-plan.md
@$HOME/.claude/topstepx/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trading-aware-agents/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt tsx-phase-researcher and tsx-research-synthesizer agents</name>
  <files>topstepx/agents/tsx-phase-researcher.md, topstepx/agents/tsx-research-synthesizer.md</files>
  <action>
**tsx-phase-researcher** -- Read GSD source: `$HOME/.claude/agents/gsd-phase-researcher.md` (555 lines).

Create `topstepx/agents/tsx-phase-researcher.md`:

Frontmatter: `name: tsx-phase-researcher`, description referencing TSX/trading phase research, `skills: tsx-phase-researcher-workflow`. Apply all mechanical naming replacements (gsd->tsx, get-shit-done->topstepx, /gsd:->/tsx:, gsd_->tsx_, absolute Windows paths->$HOME, GSD->TSX in prose, gsd-project-researcher->tsx-researcher).

Trading domain injection (~10-15 lines total):
- In `<role>` or research scope sections: Add that phase research for trading projects should consider TopStepX API capabilities, PineScript conversion patterns, trading library options, and safety pattern requirements
- In reference/source sections: Add `$HOME/.claude/topstepx/references/TOPSTEPX_API.md`, `PINESCRIPT.md`, and `safety-patterns.md` as domain references to load when researching trading phases
- In output/RESEARCH.md sections: Note that trading research should capture API endpoints needed, WebSocket subscriptions required, and safety patterns applicable

**tsx-research-synthesizer** -- Read GSD source: `$HOME/.claude/agents/gsd-research-synthesizer.md` (249 lines, smallest agent).

Create `topstepx/agents/tsx-research-synthesizer.md`:

Frontmatter: `name: tsx-research-synthesizer`, description referencing TSX/trading research synthesis, `skills: tsx-research-synthesizer-workflow`. Apply all mechanical naming replacements.

Trading domain injection (~5-10 lines, lightest touch):
- In synthesis criteria/sections: Note that trading research synthesis should surface consensus on API patterns, library choices, and safety compliance across researcher outputs
- This is the smallest agent and primarily needs naming changes

**Verify both after creation:**
- Zero GSD remnants in both files
- Valid frontmatter in both files
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-dev && test -f topstepx/agents/tsx-phase-researcher.md && test -f topstepx/agents/tsx-research-synthesizer.md && echo "BOTH_EXIST" && grep -rl "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-phase-researcher.md topstepx/agents/tsx-research-synthesizer.md 2>/dev/null | wc -l | grep -q "^0$" && echo "BOTH_CLEAN" && grep -l "name: tsx-phase-researcher" topstepx/agents/tsx-phase-researcher.md > /dev/null && echo "PHASE_RESEARCHER_OK" && grep -l "name: tsx-research-synthesizer" topstepx/agents/tsx-research-synthesizer.md > /dev/null && echo "SYNTHESIZER_OK"</automated>
  </verify>
  <done>tsx-phase-researcher.md has trading domain research context; tsx-research-synthesizer.md has trading synthesis awareness; both have zero GSD remnants</done>
</task>

<task type="auto">
  <name>Task 2: Adapt tsx-integration-checker and tsx-nyquist-auditor agents</name>
  <files>topstepx/agents/tsx-integration-checker.md, topstepx/agents/tsx-nyquist-auditor.md</files>
  <action>
**tsx-integration-checker** -- Read GSD source: `$HOME/.claude/agents/gsd-integration-checker.md` (445 lines).

Create `topstepx/agents/tsx-integration-checker.md`:

Frontmatter: `name: tsx-integration-checker`, description referencing TSX/trading integration verification, `skills: tsx-integration-checker-workflow`. Apply all mechanical naming replacements.

Trading domain injection (~15-20 lines, more than other supporting agents because integration checking is trading-relevant):
- In integration verification/checklist sections: Add trading-specific integration flows to verify:
  - Auth flow: JWT token acquisition -> storage -> refresh cycle -> authenticated API calls
  - Order flow: Contract lookup -> order construction (with brackets) -> placement -> confirmation -> position update
  - Data flow: SignalR connection -> subscription (quotes, depth, user events) -> event handling -> state update
  - Safety flow: Enum constants imported -> used in all order code -> bracket orders attached -> rate limits respected
- In cross-phase wiring sections: Note that trading phases have natural dependencies: references -> templates -> agents -> workflows -> commands (the TSX build order)

**tsx-nyquist-auditor** -- Read GSD source: `$HOME/.claude/agents/gsd-nyquist-auditor.md` (178 lines, smallest agent in the framework).

Create `topstepx/agents/tsx-nyquist-auditor.md`:

Frontmatter: `name: tsx-nyquist-auditor`, description referencing TSX test coverage auditing, `skills: tsx-nyquist-auditor-workflow`. Apply all mechanical naming replacements.

Trading domain injection (~5-10 lines, lightest touch):
- In gap analysis/test generation sections: Note that trading test coverage should verify safety patterns are tested (enum usage, bracket orders, JWT refresh, rate limits)
- This is the smallest agent and primarily needs naming changes

**Final phase-wide verification after this task (all 12 agents):**
Run the full suite to confirm all agents in topstepx/agents/ are clean:
```bash
for f in topstepx/agents/tsx-*.md; do echo "=== $f ==="; grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" "$f"; done
```
All counts must be 0.
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-dev && test -f topstepx/agents/tsx-integration-checker.md && test -f topstepx/agents/tsx-nyquist-auditor.md && echo "BOTH_EXIST" && grep -rl "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-integration-checker.md topstepx/agents/tsx-nyquist-auditor.md 2>/dev/null | wc -l | grep -q "^0$" && echo "BOTH_CLEAN" && grep -l "auth\|order\|WebSocket\|SignalR\|integration" topstepx/agents/tsx-integration-checker.md > /dev/null && echo "CHECKER_TRADING" && grep -l "name: tsx-nyquist-auditor" topstepx/agents/tsx-nyquist-auditor.md > /dev/null && echo "AUDITOR_OK"</automated>
  </verify>
  <done>tsx-integration-checker.md has trading API wiring verification (auth, order, data, safety flows); tsx-nyquist-auditor.md has trading test pattern awareness; all 12 agents complete</done>
</task>

</tasks>

<verification>
After both tasks complete, run the FULL PHASE verification (all 12 agents):

```bash
# All 12 agent files exist
ls topstepx/agents/tsx-*.md | wc -l
# Expected: 12

# Zero GSD remnants across ALL agents
grep -rl "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/ | wc -l
# Expected: 0

# Full suite: per-file count
for f in topstepx/agents/tsx-*.md; do echo "=== $(basename $f) ==="; grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" "$f"; done
# Expected: all 0

# All 12 specific files exist
for agent in executor planner researcher verifier debugger codebase-mapper plan-checker roadmapper phase-researcher research-synthesizer integration-checker nyquist-auditor; do
  test -f "topstepx/agents/tsx-${agent}.md" && echo "OK: tsx-${agent}" || echo "MISSING: tsx-${agent}"
done

# Primary agents have trading awareness
grep -l "trading\|TopStepX\|safety" topstepx/agents/tsx-executor.md topstepx/agents/tsx-planner.md topstepx/agents/tsx-researcher.md topstepx/agents/tsx-verifier.md topstepx/agents/tsx-debugger.md
```
</verification>

<success_criteria>
- All 4 agent files exist in topstepx/agents/
- All have valid YAML frontmatter with tsx-* naming
- Zero grep hits for gsd-, get-shit-done, gsd_, or C:/Users
- tsx-integration-checker has trading API wiring verification flows
- tsx-phase-researcher has trading domain research context
- tsx-research-synthesizer and tsx-nyquist-auditor have naming changes + minimal trading context
- This plan completes the full set of 12 agents for Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/03-trading-aware-agents/03-04-SUMMARY.md`
</output>
