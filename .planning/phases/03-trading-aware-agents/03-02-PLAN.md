---
phase: 03-trading-aware-agents
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - topstepx/agents/tsx-executor.md
  - topstepx/agents/tsx-verifier.md
  - topstepx/agents/tsx-researcher.md
autonomous: true
requirements: [AGT-01, AGT-03, AGT-04]

must_haves:
  truths:
    - "tsx-executor agent exists with trading safety deviation rules (never remove bracket orders, never use bare enum integers)"
    - "tsx-verifier agent exists with trading-specific verification checks (enum constants, bracket orders, JWT refresh, rate limits)"
    - "tsx-researcher agent exists with trading ecosystem research awareness (TopStepX API, PineScript, SignalR)"
    - "All three agents have zero GSD naming remnants"
    - "All three agents preserve GSD structural patterns"
  artifacts:
    - path: "topstepx/agents/tsx-executor.md"
      provides: "Trading-aware plan execution agent"
      contains: "name: tsx-executor"
    - path: "topstepx/agents/tsx-verifier.md"
      provides: "Trading-aware verification agent"
      contains: "name: tsx-verifier"
    - path: "topstepx/agents/tsx-researcher.md"
      provides: "Trading-aware research agent"
      contains: "name: tsx-researcher"
  key_links:
    - from: "topstepx/agents/tsx-executor.md"
      to: "$HOME/.claude/topstepx/references/safety-patterns.md"
      via: "reference loading for trading code execution"
      pattern: "safety-patterns"
    - from: "topstepx/agents/tsx-verifier.md"
      to: "$HOME/.claude/topstepx/references/safety-patterns.md"
      via: "verification checklist for trading compliance"
      pattern: "bracket.order\|enum.constant\|safety"
    - from: "topstepx/agents/tsx-researcher.md"
      to: "$HOME/.claude/topstepx/references/TOPSTEPX_API.md"
      via: "domain knowledge for research context"
      pattern: "TOPSTEPX_API\|TopStepX"
---

<objective>
Adapt the three remaining primary GSD agents (gsd-executor at 489 lines, gsd-verifier at 581 lines, gsd-project-researcher at 631 lines) into TSX equivalents with trading domain injection.

Purpose: These agents form the core execution triangle: executor runs plans, verifier validates results, researcher gathers domain knowledge. Each needs trading-specific awareness for safety patterns, API integration, and domain research.

Output: Three agent definition files in topstepx/agents/ with tsx-* naming, topstepx paths, trading domain awareness, and zero GSD remnants.
</objective>

<execution_context>
@$HOME/.claude/topstepx/workflows/execute-plan.md
@$HOME/.claude/topstepx/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trading-aware-agents/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt tsx-executor agent from gsd-executor</name>
  <files>topstepx/agents/tsx-executor.md</files>
  <action>
Read the GSD source: `$HOME/.claude/agents/gsd-executor.md` (489 lines).

Create `topstepx/agents/tsx-executor.md` by applying these transformations:

**Frontmatter changes:**
- `name: tsx-executor`
- `description:` "Executes TSX plans with atomic commits, deviation handling, checkpoint protocols, and state management. Trading domain aware -- loads TopStepX API, PineScript, and safety pattern references. Spawned by execute-phase orchestrator or execute-plan command."
- `skills:` Change `gsd-executor-workflow` to `tsx-executor-workflow`
- Preserve tools, color, commented hooks exactly

**Mechanical naming replacements (throughout entire file):**
- All `gsd-` agent references -> `tsx-` equivalents
- `get-shit-done` -> `topstepx` in all paths
- `gsd-tools.cjs` -> `tsx-tools.cjs`
- `/gsd:` -> `/tsx:`
- `gsd_` -> `tsx_`
- `gsd/phase-` -> `tsx/phase-`
- `C:/Users/bkevi/.claude/get-shit-done/` -> `$HOME/.claude/topstepx/`
- `GSD` -> `TSX` in prose
- `gsd-project-researcher` -> `tsx-researcher`

**Trading domain injection (add to existing sections):**
- In `<role>`: Add trading domain awareness block (see 03-RESEARCH.md Example 3):
  - Load `$HOME/.claude/topstepx/references/safety-patterns.md` for any task creating order/position/API code
  - Verify enum values use named constants (SAF-01)
  - Ensure bracket orders (stop-loss + take-profit) in order placement code (SAF-01)
  - Verify JWT token refresh patterns (SAF-02)
  - Check rate limit compliance (SAF-03)
- In deviation handling section: Add trading safety deviations that are NEVER acceptable:
  - Removing bracket orders from order placement code
  - Using bare integer enum values instead of named constants
  - Skipping JWT refresh in auth code
  - Exceeding rate limits in API call patterns

**Verify after creation:**
- `grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-executor.md` must return 0
- `grep -c "trading\|TopStepX\|safety" topstepx/agents/tsx-executor.md` must be > 0
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-skill && test -f topstepx/agents/tsx-executor.md && echo "EXISTS" && grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-executor.md | grep -q "^0$" && echo "CLEAN" && grep -l "trading\|TopStepX\|safety" topstepx/agents/tsx-executor.md > /dev/null && echo "TRADING_AWARE"</automated>
  </verify>
  <done>tsx-executor.md exists with tsx-* naming, zero GSD remnants, and trading safety deviation rules</done>
</task>

<task type="auto">
  <name>Task 2: Adapt tsx-verifier and tsx-researcher agents</name>
  <files>topstepx/agents/tsx-verifier.md, topstepx/agents/tsx-researcher.md</files>
  <action>
**tsx-verifier** -- Read GSD source: `$HOME/.claude/agents/gsd-verifier.md` (581 lines).

Create `topstepx/agents/tsx-verifier.md`:

Frontmatter: `name: tsx-verifier`, description referencing TSX/trading verification, `skills: tsx-verifier-workflow`. Apply all mechanical naming replacements.

Trading domain injection:
- In `<role>`: Add that verifier checks trading implementations against safety requirements
- In verification checklist/criteria sections: Add trading-specific verification items:
  - All OrderSide, OrderType, OrderStatus values use named enum constants (grep for bare integers 0-7 in order code)
  - Order placement includes stopLossBracket and takeProfitBracket
  - JWT token storage includes expiry tracking and refresh via /api/Auth/validate
  - API calls respect rate limits (50/30s for history, 200/60s general)
  - SignalR connections use WebSocket transport with skipNegotiation:true and auto-reconnect
  - PineScript conversions use barstate.isconfirmed (bar-close default)
- In must_haves derivation: Note trading-specific truths to verify

**tsx-researcher** -- Read GSD source: `$HOME/.claude/agents/gsd-project-researcher.md` (631 lines).

Create `topstepx/agents/tsx-researcher.md`:

Frontmatter: `name: tsx-researcher`, description referencing TSX/trading research, `skills: tsx-researcher-workflow`. Apply all mechanical naming replacements. Note: source is `gsd-project-researcher` but target is `tsx-researcher` per REQUIREMENTS.md AGT-03.

Trading domain injection:
- In `<role>`: Add trading ecosystem research context -- TopStepX API capabilities, PineScript conversion patterns, trading library options (trading-signals, pandas-ta, pysignalr)
- In research domain sections: Add awareness of TopStepX-specific research areas (API authentication patterns, WebSocket subscription models, order flow architectures)
- In reference/source sections: Add `$HOME/.claude/topstepx/references/TOPSTEPX_API.md` and `PINESCRIPT.md` as primary domain references

**Verify both after creation:**
- Zero GSD remnants in both files
- Trading awareness in both files
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-skill && test -f topstepx/agents/tsx-verifier.md && test -f topstepx/agents/tsx-researcher.md && echo "BOTH_EXIST" && grep -rl "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-verifier.md topstepx/agents/tsx-researcher.md 2>/dev/null | wc -l | grep -q "^0$" && echo "BOTH_CLEAN" && grep -l "enum\|bracket\|safety" topstepx/agents/tsx-verifier.md > /dev/null && echo "VERIFIER_TRADING" && grep -l "TopStepX\|trading\|PineScript" topstepx/agents/tsx-researcher.md > /dev/null && echo "RESEARCHER_TRADING"</automated>
  </verify>
  <done>tsx-verifier.md has trading verification checklist (enums, brackets, JWT, rate limits); tsx-researcher.md has trading ecosystem research awareness (TopStepX API, PineScript, libraries)</done>
</task>

</tasks>

<verification>
After both tasks complete, run full verification:

```bash
# All files exist
ls -la topstepx/agents/tsx-executor.md topstepx/agents/tsx-verifier.md topstepx/agents/tsx-researcher.md

# Zero GSD remnants
grep -rl "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-executor.md topstepx/agents/tsx-verifier.md topstepx/agents/tsx-researcher.md | wc -l
# Expected: 0

# Trading awareness
grep -l "trading\|TopStepX\|safety" topstepx/agents/tsx-executor.md
grep -l "enum\|bracket\|safety" topstepx/agents/tsx-verifier.md
grep -l "TopStepX\|PineScript" topstepx/agents/tsx-researcher.md
```
</verification>

<success_criteria>
- tsx-executor.md, tsx-verifier.md, tsx-researcher.md exist in topstepx/agents/
- All three have valid YAML frontmatter with tsx-* naming
- Zero grep hits for gsd-, get-shit-done, gsd_, or C:/Users in any file
- tsx-executor has trading safety deviation rules
- tsx-verifier has trading-specific verification checklist
- tsx-researcher has trading ecosystem research awareness
- All three preserve GSD structural patterns
</success_criteria>

<output>
After completion, create `.planning/phases/03-trading-aware-agents/03-02-SUMMARY.md`
</output>
