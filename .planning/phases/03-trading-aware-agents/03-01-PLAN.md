---
phase: 03-trading-aware-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - topstepx/agents/tsx-planner.md
  - topstepx/agents/tsx-debugger.md
autonomous: true
requirements: [AGT-02, AGT-05]

must_haves:
  truths:
    - "tsx-planner agent exists with trading-aware plan creation guidance"
    - "tsx-debugger agent exists with trading-specific debug patterns (JWT, rate limits, WebSocket, order rejections)"
    - "Both agents have zero GSD naming remnants (no gsd-, get-shit-done, gsd_, C:/Users paths)"
    - "Both agents preserve GSD structural patterns (XML tags, execution flow, frontmatter schema)"
  artifacts:
    - path: "topstepx/agents/tsx-planner.md"
      provides: "Trading-aware plan creation agent"
      contains: "name: tsx-planner"
    - path: "topstepx/agents/tsx-debugger.md"
      provides: "Trading-aware debug agent"
      contains: "name: tsx-debugger"
  key_links:
    - from: "topstepx/agents/tsx-planner.md"
      to: "$HOME/.claude/topstepx/bin/tsx-tools.cjs"
      via: "CLI command references in execution flow"
      pattern: "tsx-tools\\.cjs"
    - from: "topstepx/agents/tsx-debugger.md"
      to: "$HOME/.claude/topstepx/references/safety-patterns.md"
      via: "reference loading for trading debug context"
      pattern: "safety-patterns"
---

<objective>
Adapt the two largest GSD agents (gsd-planner at 1,309 lines, gsd-debugger at 1,257 lines) into TSX equivalents with full trading domain injection.

Purpose: These are the two most complex agents in the framework. The planner drives all phase planning with trading-aware task breakdown. The debugger handles trading-specific issues (JWT expiry, rate limits, WebSocket drops, order failures).

Output: Two agent definition files in topstepx/agents/ with tsx-* naming, topstepx paths, trading domain awareness, and zero GSD remnants.
</objective>

<execution_context>
@$HOME/.claude/topstepx/workflows/execute-plan.md
@$HOME/.claude/topstepx/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trading-aware-agents/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt tsx-planner agent from gsd-planner</name>
  <files>topstepx/agents/tsx-planner.md</files>
  <action>
Read the GSD source: `$HOME/.claude/agents/gsd-planner.md` (1,309 lines).

Create `topstepx/agents/tsx-planner.md` by applying these transformations:

**Frontmatter changes:**
- `name: tsx-planner`
- `description:` Update to reference TSX/trading context instead of GSD
- `skills:` Change `gsd-planner-workflow` to `tsx-planner-workflow`
- Preserve tools, color, commented hooks exactly

**Mechanical naming replacements (throughout entire file):**
- `gsd-planner` -> `tsx-planner` (and all other `gsd-{agent}` -> `tsx-{agent}` references)
- `get-shit-done` -> `topstepx` in all paths
- `gsd-tools.cjs` -> `tsx-tools.cjs` in all CLI commands
- `/gsd:` -> `/tsx:` in all command references
- `gsd_` -> `tsx_` in variable names
- `gsd/phase-` -> `tsx/phase-` in branch template references
- `C:/Users/bkevi/.claude/get-shit-done/` -> `$HOME/.claude/topstepx/` (all absolute Windows paths)
- `GSD` -> `TSX` in prose (e.g., "GSD plan executor" -> "TSX plan executor")
- `gsd-project-researcher` -> `tsx-researcher` (per AGT-03 naming)

**Trading domain injection (add to existing sections, do NOT add new mega-sections):**
- In `<role>`: Add 5-8 lines about trading domain awareness: planner validates safety pattern inclusion in plans, ensures bracket orders are non-optional in any order-placement task, verifies enum constant usage requirements
- In discovery/research sections: Add awareness of TopStepX API, PineScript conversion, and trading library ecosystems as research domains
- In plan template/example sections: Note that trading-specific templates exist at `$HOME/.claude/topstepx/templates/` (strategy-spec, api-integration-plan, risk-parameters, bot-scaffolds)
- In must_haves/verification sections: Add trading-specific verification criteria (safety patterns, enum constants, bracket orders)

**Do NOT:**
- Restructure XML tags, reorder sections, or add new structural patterns
- Add more than ~30-50 lines of trading-specific content total
- Change the fundamental execution flow or philosophy

**Verify after creation:**
- `grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-planner.md` must return 0
- `grep -c "tsx-planner" topstepx/agents/tsx-planner.md` must be > 0
- `grep -c "trading\|TopStepX\|safety" topstepx/agents/tsx-planner.md` must be > 0
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-skill && test -f topstepx/agents/tsx-planner.md && echo "EXISTS" && grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-planner.md | grep -q "^0$" && echo "CLEAN" && grep -l "trading\|TopStepX\|safety" topstepx/agents/tsx-planner.md > /dev/null && echo "TRADING_AWARE" && grep -l "name: tsx-planner" topstepx/agents/tsx-planner.md > /dev/null && echo "FRONTMATTER_OK"</automated>
  </verify>
  <done>tsx-planner.md exists with tsx-* naming, topstepx paths, zero GSD remnants, and trading-aware plan creation guidance in role/verification sections</done>
</task>

<task type="auto">
  <name>Task 2: Adapt tsx-debugger agent from gsd-debugger</name>
  <files>topstepx/agents/tsx-debugger.md</files>
  <action>
Read the GSD source: `$HOME/.claude/agents/gsd-debugger.md` (1,257 lines).

Create `topstepx/agents/tsx-debugger.md` by applying the same mechanical naming replacements as Task 1 (all gsd->tsx, get-shit-done->topstepx, /gsd:->/tsx:, absolute Windows paths->$HOME, etc.).

**Frontmatter changes:**
- `name: tsx-debugger`
- `description:` Reference TSX/trading debug context
- `skills:` Change `gsd-debugger-workflow` to `tsx-debugger-workflow`
- Preserve tools (including WebSearch), color, commented hooks

**Trading domain injection (add to existing sections):**
- In `<role>`: Add 5-8 lines about trading-specific debugging awareness: JWT token expiry (24hr), rate limit 429 errors (50/30s history, 200/60s general), order rejection codes, WebSocket/SignalR reconnection failures, enum mismatch issues
- In debugging methodology/patterns sections: Add a trading-specific debug checklist:
  - JWT expired? Check token age vs 24hr expiry, call /api/Auth/validate
  - Rate limited? Check request frequency against TopStepX limits
  - Order rejected? Check OrderType/OrderSide enum values (must be named constants, not bare integers)
  - WebSocket disconnected? Check SignalR transport (must be WebSocket with skipNegotiation:true)
  - Position mismatch? Check bracket order (stopLoss + takeProfit) requirements
- In error pattern sections: Add TopStepX-specific error patterns (401 JWT expired, 429 rate limit, order rejection response shapes)

**Do NOT:**
- Add more than ~30-50 lines of trading content total
- Restructure the scientific debugging methodology or session management patterns
- Change the checkpoint handling or deviation protocols

**Verify after creation:**
- `grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-debugger.md` must return 0
- `grep -c "trading\|TopStepX\|JWT\|rate.limit\|WebSocket\|SignalR" topstepx/agents/tsx-debugger.md` must be > 0
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-skill && test -f topstepx/agents/tsx-debugger.md && echo "EXISTS" && grep -c "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-debugger.md | grep -q "^0$" && echo "CLEAN" && grep -l "JWT\|rate.limit\|WebSocket\|SignalR" topstepx/agents/tsx-debugger.md > /dev/null && echo "TRADING_DEBUG" && grep -l "name: tsx-debugger" topstepx/agents/tsx-debugger.md > /dev/null && echo "FRONTMATTER_OK"</automated>
  </verify>
  <done>tsx-debugger.md exists with tsx-* naming, topstepx paths, zero GSD remnants, and trading-specific debug patterns (JWT, rate limits, WebSocket, order rejections)</done>
</task>

</tasks>

<verification>
After both tasks complete, run full verification:

```bash
# All files exist
ls -la topstepx/agents/tsx-planner.md topstepx/agents/tsx-debugger.md

# Zero GSD remnants in either file
grep -rl "gsd-\|get-shit-done\|gsd_\|C:/Users" topstepx/agents/tsx-planner.md topstepx/agents/tsx-debugger.md | wc -l
# Expected: 0

# Trading awareness present
grep -l "trading\|TopStepX\|safety" topstepx/agents/tsx-planner.md
grep -l "JWT\|rate.limit\|WebSocket\|SignalR" topstepx/agents/tsx-debugger.md

# Frontmatter valid
head -5 topstepx/agents/tsx-planner.md topstepx/agents/tsx-debugger.md
```
</verification>

<success_criteria>
- tsx-planner.md and tsx-debugger.md exist in topstepx/agents/
- Both files have valid YAML frontmatter with tsx-* naming
- Zero grep hits for gsd-, get-shit-done, gsd_, or C:/Users in either file
- tsx-planner contains trading-aware plan validation guidance
- tsx-debugger contains trading-specific debug patterns (JWT, rate limits, WebSocket, order failures)
- Both files preserve GSD structural patterns (same XML tags, same execution flow structure)
</success_criteria>

<output>
After completion, create `.planning/phases/03-trading-aware-agents/03-01-SUMMARY.md`
</output>
