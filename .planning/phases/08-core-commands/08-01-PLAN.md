---
phase: 08-core-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/tsx/new-project.md
  - commands/tsx/discuss-phase.md
  - commands/tsx/plan-phase.md
  - commands/tsx/execute-phase.md
  - commands/tsx/verify-work.md
  - commands/tsx/audit-milestone.md
  - commands/tsx/complete-milestone.md
  - commands/tsx/new-milestone.md
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03, CMD-04, CMD-05, CMD-06, CMD-07, CMD-08]

must_haves:
  truths:
    - "User can invoke /tsx:new-project to start a trading bot project"
    - "User can invoke /tsx:discuss-phase to gather phase context"
    - "User can invoke /tsx:plan-phase with tsx-planner agent routing"
    - "User can invoke /tsx:execute-phase to run wave-based execution"
    - "User can invoke /tsx:verify-work for conversational UAT"
    - "User can invoke /tsx:audit-milestone, /tsx:complete-milestone, /tsx:new-milestone for lifecycle management"
    - "Every command delegates to its corresponding topstepx/workflows/ file with zero inline logic"
    - "No GSD naming (gsd:, get-shit-done, gsd-*) leaks into any command file"
  artifacts:
    - path: "commands/tsx/new-project.md"
      provides: "/tsx:new-project command entry point"
      contains: "name: tsx:new-project"
    - path: "commands/tsx/discuss-phase.md"
      provides: "/tsx:discuss-phase command entry point"
      contains: "name: tsx:discuss-phase"
    - path: "commands/tsx/plan-phase.md"
      provides: "/tsx:plan-phase command entry point with agent field"
      contains: "agent: tsx-planner"
    - path: "commands/tsx/execute-phase.md"
      provides: "/tsx:execute-phase command entry point"
      contains: "name: tsx:execute-phase"
    - path: "commands/tsx/verify-work.md"
      provides: "/tsx:verify-work command entry point"
      contains: "name: tsx:verify-work"
    - path: "commands/tsx/audit-milestone.md"
      provides: "/tsx:audit-milestone command entry point"
      contains: "name: tsx:audit-milestone"
    - path: "commands/tsx/complete-milestone.md"
      provides: "/tsx:complete-milestone command with type: prompt"
      contains: "type: prompt"
    - path: "commands/tsx/new-milestone.md"
      provides: "/tsx:new-milestone command entry point"
      contains: "name: tsx:new-milestone"
  key_links:
    - from: "commands/tsx/new-project.md"
      to: "topstepx/workflows/new-project.md"
      via: "@ reference in execution_context"
      pattern: "topstepx/workflows/new-project\\.md"
    - from: "commands/tsx/plan-phase.md"
      to: "topstepx/workflows/plan-phase.md"
      via: "@ reference in execution_context"
      pattern: "topstepx/workflows/plan-phase\\.md"
    - from: "commands/tsx/execute-phase.md"
      to: "topstepx/workflows/execute-phase.md"
      via: "@ reference in execution_context"
      pattern: "topstepx/workflows/execute-phase\\.md"
---

<objective>
Create the 8 core workflow commands (CMD-01 through CMD-08) as thin Markdown entry points in commands/tsx/ that delegate to existing topstepx/workflows/ files.

Purpose: Give users /tsx:new-project, /tsx:discuss-phase, /tsx:plan-phase, /tsx:execute-phase, /tsx:verify-work, /tsx:audit-milestone, /tsx:complete-milestone, and /tsx:new-milestone slash commands that invoke the corresponding workflows.

Output: 8 command .md files in commands/tsx/
</objective>

<execution_context>
@C:/Users/bkevi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bkevi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-commands/08-RESEARCH.md

Each command follows the exact same pattern: YAML frontmatter (name, description, argument-hint, allowed-tools, optionally agent/type) followed by XML-like sections (context, objective, execution_context, process, and optionally success_criteria/critical_rules).

The GSD source commands are the structural templates. The adaptation is mechanical:
- `gsd:` -> `tsx:` in name fields and routing text
- `get-shit-done` -> `topstepx` in @ reference paths
- `gsd-tools.cjs` -> `tsx-tools.cjs` in any bash references
- `gsd-planner` -> `tsx-planner` in agent field
- `GSD` -> `TSX` in descriptive text
- `/gsd:` -> `/tsx:` in next-step routing
- Absolute user paths (`C:/Users/bkevi/.claude/get-shit-done/`) -> `$HOME/.claude/topstepx/`
- Description text updated to reference trading/TopStepX where appropriate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 primary workflow commands (new-project, discuss-phase, plan-phase, execute-phase)</name>
  <files>commands/tsx/new-project.md, commands/tsx/discuss-phase.md, commands/tsx/plan-phase.md, commands/tsx/execute-phase.md</files>
  <action>
Create 4 command files by adapting the GSD source commands at ~/.claude/commands/gsd/ with TSX naming.

**commands/tsx/new-project.md** (CMD-01):
- Frontmatter: name: tsx:new-project, description: "Initialize a new TopStepX trading bot project with domain-specific questioning", argument-hint: "[--auto]", allowed-tools: [Read, Bash, Write, Task, AskUserQuestion]
- Sections: context (--auto flag docs), objective (creates .planning/ files, after-command routes to /tsx:plan-phase 1), execution_context (@ refs to topstepx/workflows/new-project.md, topstepx/references/questioning.md, topstepx/references/ui-brand.md, topstepx/templates/project.md, topstepx/templates/requirements.md), process (execute workflow end-to-end)
- All @ paths use $HOME/.claude/topstepx/ prefix

**commands/tsx/discuss-phase.md** (CMD-02):
- Frontmatter: name: tsx:discuss-phase, description: "Gather phase context through adaptive questioning before planning", argument-hint: "<phase> [--auto]", allowed-tools: [Read, Write, Bash, Glob, Grep, AskUserQuestion, Task, mcp__context7__resolve-library-id, mcp__context7__query-docs]
- Sections: objective (extract implementation decisions, output {phase_num}-CONTEXT.md), execution_context (@ refs to topstepx/workflows/discuss-phase.md, topstepx/templates/context.md), context (phase number from $ARGUMENTS), process (full GSD discuss-phase process adapted with /tsx: routing and topstepx paths), success_criteria
- Preserve the full process section content from GSD source but replace all gsd/GSD references with tsx/TSX

**commands/tsx/plan-phase.md** (CMD-03):
- Frontmatter: name: tsx:plan-phase, description: "Create detailed phase plan (PLAN.md) with verification loop", argument-hint: "[phase] [--auto] [--research] [--skip-research] [--gaps] [--skip-verify] [--prd <file>]", agent: tsx-planner, allowed-tools: [Read, Write, Bash, Glob, Grep, Task, WebFetch, mcp__context7__*]
- CRITICAL: Must include `agent: tsx-planner` in frontmatter
- Sections: objective (create executable phase prompts, orchestrator role mentions tsx-planner and tsx-plan-checker), execution_context (@ refs to topstepx/workflows/plan-phase.md, topstepx/references/ui-brand.md), context (phase number, flags), process (execute workflow end-to-end)

**commands/tsx/execute-phase.md** (CMD-04):
- Frontmatter: name: tsx:execute-phase, description: "Execute all plans in a phase with wave-based parallelization", argument-hint: "<phase-number> [--gaps-only]", allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Task, TodoWrite, AskUserQuestion]
- Sections: objective (wave-based parallel execution, context budget ~15% orchestrator), execution_context (@ refs to topstepx/workflows/execute-phase.md, topstepx/references/ui-brand.md), context (phase from $ARGUMENTS, --gaps-only flag, tsx-tools init reference), process (execute workflow end-to-end)

After creating all 4 files, verify zero GSD naming leaks with: grep -r "gsd\|get-shit-done" commands/tsx/new-project.md commands/tsx/discuss-phase.md commands/tsx/plan-phase.md commands/tsx/execute-phase.md
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/topstepx-dev && ls commands/tsx/new-project.md commands/tsx/discuss-phase.md commands/tsx/plan-phase.md commands/tsx/execute-phase.md && grep "agent: tsx-planner" commands/tsx/plan-phase.md && grep -rn "gsd\|get-shit-done" commands/tsx/new-project.md commands/tsx/discuss-phase.md commands/tsx/plan-phase.md commands/tsx/execute-phase.md; echo "GSD leak check exit: $?"</automated>
  </verify>
  <done>4 command files exist with correct frontmatter, correct @ reference paths using $HOME/.claude/topstepx/, plan-phase has agent: tsx-planner, zero GSD naming leaks</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 lifecycle commands (verify-work, audit-milestone, complete-milestone, new-milestone)</name>
  <files>commands/tsx/verify-work.md, commands/tsx/audit-milestone.md, commands/tsx/complete-milestone.md, commands/tsx/new-milestone.md</files>
  <action>
Create 4 command files by adapting the GSD source commands with TSX naming.

**commands/tsx/verify-work.md** (CMD-05):
- Frontmatter: name: tsx:verify-work, description: "Validate built features through conversational UAT", argument-hint: "[phase number, e.g., '4']", allowed-tools: [Read, Bash, Glob, Grep, Edit, Write, Task]
- Sections: objective (validate features, output {phase_num}-UAT.md, mentions /tsx:execute-phase for fix execution), execution_context (@ refs to topstepx/workflows/verify-work.md, topstepx/templates/UAT.md), context (phase from $ARGUMENTS), process (execute workflow end-to-end)

**commands/tsx/audit-milestone.md** (CMD-06):
- Frontmatter: name: tsx:audit-milestone, description: "Audit milestone completion against original intent before archiving", argument-hint: "[version]", allowed-tools: [Read, Glob, Grep, Bash, Task, Write]
- Sections: objective (verify milestone DoD, mentions tsx-integration-checker), execution_context (@ ref to topstepx/workflows/audit-milestone.md), context (version from $ARGUMENTS, glob paths for SUMMARY.md and VERIFICATION.md), process (execute workflow end-to-end)

**commands/tsx/complete-milestone.md** (CMD-07):
- Frontmatter: MUST include `type: prompt` as first field. name: tsx:complete-milestone, description: "Archive completed milestone and prepare for next version", argument-hint: "<version>", allowed-tools: [Read, Write, Bash]
- CRITICAL: Must have `type: prompt` in frontmatter (GSD convention preserved exactly)
- Sections: objective (mark milestone complete, archive, git tag), execution_context with "Load these files NOW" phrasing (@ refs to topstepx/workflows/complete-milestone.md, topstepx/templates/milestone-archive.md), context (project files list, user input version), process (full 8-step process from GSD source with all /gsd: replaced with /tsx:), success_criteria, critical_rules
- This is the longest command -- preserve all GSD process detail, just replace naming

**commands/tsx/new-milestone.md** (CMD-08):
- Frontmatter: name: tsx:new-milestone, description: "Start a new milestone cycle -- update PROJECT.md and route to requirements", argument-hint: "[milestone name, e.g., 'v1.1 Notifications']", allowed-tools: [Read, Write, Bash, Task, AskUserQuestion]
- Sections: objective (brownfield new-project, creates/updates planning files, after-command routes to /tsx:plan-phase), execution_context (@ refs to topstepx/workflows/new-milestone.md, topstepx/references/questioning.md, topstepx/references/ui-brand.md, topstepx/templates/project.md, topstepx/templates/requirements.md), context (milestone name from $ARGUMENTS), process (execute workflow end-to-end)

After creating all 4 files, verify zero GSD naming leaks and type: prompt on complete-milestone.
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/topstepx-dev && ls commands/tsx/verify-work.md commands/tsx/audit-milestone.md commands/tsx/complete-milestone.md commands/tsx/new-milestone.md && grep "type: prompt" commands/tsx/complete-milestone.md && grep -rn "gsd\|get-shit-done" commands/tsx/verify-work.md commands/tsx/audit-milestone.md commands/tsx/complete-milestone.md commands/tsx/new-milestone.md; echo "GSD leak check exit: $?"</automated>
  </verify>
  <done>4 command files exist with correct frontmatter, complete-milestone has type: prompt, all @ paths use $HOME/.claude/topstepx/, zero GSD naming leaks, all route to correct topstepx/workflows/ files</done>
</task>

</tasks>

<verification>
All 8 core workflow commands exist and are correctly structured:
- `ls commands/tsx/*.md | wc -l` returns at least 8
- `grep -rn "gsd\|get-shit-done" commands/tsx/` returns zero matches
- `grep "agent: tsx-planner" commands/tsx/plan-phase.md` confirms agent field
- `grep "type: prompt" commands/tsx/complete-milestone.md` confirms type field
- Each command references its correct workflow via @ path in execution_context
</verification>

<success_criteria>
- 8 command files exist in commands/tsx/ (new-project, discuss-phase, plan-phase, execute-phase, verify-work, audit-milestone, complete-milestone, new-milestone)
- Every command has valid YAML frontmatter with name, description, and allowed-tools
- plan-phase.md has `agent: tsx-planner`
- complete-milestone.md has `type: prompt`
- All @ reference paths use $HOME/.claude/topstepx/ prefix
- Zero instances of "gsd", "get-shit-done", "/gsd:", or "gsd-" in any command file
- Each command delegates to its matching workflow (zero inline orchestration logic)
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-commands/08-01-SUMMARY.md`
</output>
