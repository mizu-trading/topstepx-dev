---
phase: 08-core-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/tsx/progress.md
  - commands/tsx/resume-work.md
  - commands/tsx/pause-work.md
  - commands/tsx/help.md
autonomous: true
requirements: [CMD-09, CMD-10, CMD-11, CMD-12]

must_haves:
  truths:
    - "User can invoke /tsx:progress to see project status and route to next action"
    - "User can invoke /tsx:resume-work to restore context from a previous session"
    - "User can invoke /tsx:pause-work to save handoff state mid-phase"
    - "User can invoke /tsx:help to see all available TSX commands"
    - "Every command delegates to its corresponding topstepx/workflows/ file with zero inline logic"
    - "No GSD naming (gsd:, get-shit-done, gsd-*) leaks into any command file"
  artifacts:
    - path: "commands/tsx/progress.md"
      provides: "/tsx:progress command entry point"
      contains: "name: tsx:progress"
    - path: "commands/tsx/resume-work.md"
      provides: "/tsx:resume-work command entry point"
      contains: "name: tsx:resume-work"
    - path: "commands/tsx/pause-work.md"
      provides: "/tsx:pause-work command entry point"
      contains: "name: tsx:pause-work"
    - path: "commands/tsx/help.md"
      provides: "/tsx:help command with no allowed-tools"
      contains: "name: tsx:help"
  key_links:
    - from: "commands/tsx/progress.md"
      to: "topstepx/workflows/progress.md"
      via: "@ reference in execution_context"
      pattern: "topstepx/workflows/progress\\.md"
    - from: "commands/tsx/resume-work.md"
      to: "topstepx/workflows/resume-project.md"
      via: "@ reference in execution_context (note: resume-work maps to resume-project)"
      pattern: "topstepx/workflows/resume-project\\.md"
    - from: "commands/tsx/help.md"
      to: "topstepx/workflows/help.md"
      via: "@ reference in execution_context"
      pattern: "topstepx/workflows/help\\.md"
---

<objective>
Create the 4 navigation commands (CMD-09 through CMD-12) as thin Markdown entry points in commands/tsx/ that delegate to existing topstepx/workflows/ files.

Purpose: Give users /tsx:progress, /tsx:resume-work, /tsx:pause-work, and /tsx:help slash commands for project navigation and discovery.

Output: 4 command .md files in commands/tsx/
</objective>

<execution_context>
@C:/Users/bkevi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bkevi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-commands/08-RESEARCH.md

Same mechanical adaptation pattern as Plan 01. Key differences for navigation commands:
- progress.md and resume-work.md use SlashCommand in allowed-tools (for routing to next action)
- help.md has NO allowed-tools at all (just displays reference content)
- resume-work.md maps to resume-project.md workflow (intentional GSD naming mismatch preserved)
- These are generally slimmer commands (5-25 lines of content after frontmatter)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 navigation commands (progress, resume-work, pause-work, help)</name>
  <files>commands/tsx/progress.md, commands/tsx/resume-work.md, commands/tsx/pause-work.md, commands/tsx/help.md</files>
  <action>
Create 4 command files by adapting the GSD source commands at ~/.claude/commands/gsd/ with TSX naming.

**commands/tsx/progress.md** (CMD-09):
- Frontmatter: name: tsx:progress, description: "Check project progress, show context, and route to next action", allowed-tools: [Read, Bash, Grep, Glob, SlashCommand]
- Note: No argument-hint (progress takes no arguments)
- Sections: objective (check progress, summarize recent work, route to next action), execution_context (@ ref to topstepx/workflows/progress.md), process (execute workflow, preserve routing logic Routes A through F)

**commands/tsx/resume-work.md** (CMD-10):
- Frontmatter: name: tsx:resume-work, description: "Resume work from previous session with full context restoration", allowed-tools: [Read, Bash, Write, AskUserQuestion, SlashCommand]
- Note: No argument-hint (resume takes no arguments)
- CRITICAL: execution_context must reference topstepx/workflows/resume-project.md (NOT resume-work.md) -- this is an intentional naming mismatch from GSD convention
- Sections: objective (restore context, mentions STATE.md loading, checkpoint detection, incomplete work detection, next action routing), execution_context (@ ref to topstepx/workflows/resume-project.md), process (follow resume-project workflow, 7 steps listed)

**commands/tsx/pause-work.md** (CMD-11):
- Frontmatter: name: tsx:pause-work, description: "Create context handoff when pausing work mid-phase", allowed-tools: [Read, Write, Bash]
- Note: No argument-hint
- Sections: objective (create .continue-here.md handoff, mentions state gathering, git commit, resume instructions), execution_context (@ ref to topstepx/workflows/pause-work.md), context (state and phase gathered in-workflow), process (follow pause-work workflow, 5 steps listed)

**commands/tsx/help.md** (CMD-12):
- Frontmatter: name: tsx:help, description: "Show available TSX commands and usage guide"
- CRITICAL: NO allowed-tools field at all (help is display-only, needs no tools)
- Sections: objective (display complete TSX command reference, explicit DO NOT add list: project analysis, git status, next-step suggestions, commentary), execution_context (@ ref to topstepx/workflows/help.md), process (output reference content directly, no additions)

After creating all 4 files, verify:
1. Zero GSD naming leaks: grep -r "gsd\|get-shit-done" commands/tsx/progress.md commands/tsx/resume-work.md commands/tsx/pause-work.md commands/tsx/help.md
2. resume-work.md references resume-project.md workflow: grep "resume-project" commands/tsx/resume-work.md
3. help.md has no allowed-tools: grep "allowed-tools" commands/tsx/help.md (should return nothing)
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/topstepx-dev && ls commands/tsx/progress.md commands/tsx/resume-work.md commands/tsx/pause-work.md commands/tsx/help.md && grep "resume-project" commands/tsx/resume-work.md && grep -c "allowed-tools" commands/tsx/help.md | grep "^0$" && grep -rn "gsd\|get-shit-done" commands/tsx/progress.md commands/tsx/resume-work.md commands/tsx/pause-work.md commands/tsx/help.md; echo "GSD leak check exit: $?"</automated>
  </verify>
  <done>4 navigation command files exist, resume-work.md references resume-project.md workflow, help.md has no allowed-tools, zero GSD naming leaks across all files</done>
</task>

<task type="auto">
  <name>Task 2: Validate all 12 Phase 8 commands end-to-end</name>
  <files>commands/tsx/progress.md, commands/tsx/resume-work.md, commands/tsx/pause-work.md, commands/tsx/help.md</files>
  <action>
Run comprehensive validation across ALL 12 command files (8 from Plan 01 + 4 from this plan):

1. **File count check:** `ls commands/tsx/*.md | wc -l` must equal 12

2. **GSD leak check:** `grep -rn "gsd\|get-shit-done" commands/tsx/` must return zero matches

3. **Frontmatter validation for each file:** Verify each command has correct `name: tsx:{command-name}` field:
   - grep "name: tsx:new-project" commands/tsx/new-project.md
   - grep "name: tsx:discuss-phase" commands/tsx/discuss-phase.md
   - grep "name: tsx:plan-phase" commands/tsx/plan-phase.md
   - grep "name: tsx:execute-phase" commands/tsx/execute-phase.md
   - grep "name: tsx:verify-work" commands/tsx/verify-work.md
   - grep "name: tsx:audit-milestone" commands/tsx/audit-milestone.md
   - grep "name: tsx:complete-milestone" commands/tsx/complete-milestone.md
   - grep "name: tsx:new-milestone" commands/tsx/new-milestone.md
   - grep "name: tsx:progress" commands/tsx/progress.md
   - grep "name: tsx:resume-work" commands/tsx/resume-work.md
   - grep "name: tsx:pause-work" commands/tsx/pause-work.md
   - grep "name: tsx:help" commands/tsx/help.md

4. **Special field checks:**
   - grep "agent: tsx-planner" commands/tsx/plan-phase.md (must match)
   - grep "type: prompt" commands/tsx/complete-milestone.md (must match)
   - grep -c "allowed-tools" commands/tsx/help.md (must be 0)

5. **Workflow reference validation:** For each command, verify it references a workflow file that exists in topstepx/workflows/:
   - Extract all @ references from commands/tsx/*.md
   - Verify each referenced workflow path exists relative to topstepx/workflows/

6. **Line count check:** `wc -l commands/tsx/*.md` -- each file should be under 140 lines (complete-milestone is longest due to full process section; most are 20-45 lines)

If any check fails, fix the offending file before completing.
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/topstepx-dev && echo "=== File count ===" && ls commands/tsx/*.md | wc -l && echo "=== GSD leaks ===" && grep -rn "gsd\|get-shit-done" commands/tsx/ || echo "No GSD leaks found" && echo "=== Special fields ===" && grep "agent: tsx-planner" commands/tsx/plan-phase.md && grep "type: prompt" commands/tsx/complete-milestone.md && echo "=== Help no tools ===" && grep -c "allowed-tools" commands/tsx/help.md && echo "=== Resume-work mapping ===" && grep "resume-project" commands/tsx/resume-work.md</automated>
  </verify>
  <done>All 12 command files validated: correct count, zero GSD leaks, all special frontmatter fields present, help has no allowed-tools, resume-work maps to resume-project workflow, all referenced workflow files exist</done>
</task>

</tasks>

<verification>
All 4 navigation commands plus cross-plan validation of all 12 commands:
- `ls commands/tsx/*.md | wc -l` returns 12
- `grep -rn "gsd\|get-shit-done" commands/tsx/` returns zero matches
- Each command file has correct `name: tsx:*` frontmatter
- plan-phase.md has `agent: tsx-planner`
- complete-milestone.md has `type: prompt`
- help.md has no `allowed-tools`
- resume-work.md references resume-project.md workflow
- All workflow @ references point to files that exist in topstepx/workflows/
</verification>

<success_criteria>
- 4 navigation command files exist in commands/tsx/ (progress, resume-work, pause-work, help)
- All 12 Phase 8 commands collectively pass the full validation suite
- Every command has valid YAML frontmatter with name and description
- progress.md and resume-work.md include SlashCommand in allowed-tools
- help.md has no allowed-tools field
- resume-work.md maps to topstepx/workflows/resume-project.md (not resume-work.md)
- All @ reference paths use $HOME/.claude/topstepx/ prefix
- Zero instances of "gsd", "get-shit-done", "/gsd:", or "gsd-" in any command file
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-commands/08-02-SUMMARY.md`
</output>
