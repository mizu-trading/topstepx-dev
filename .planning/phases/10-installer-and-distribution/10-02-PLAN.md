---
phase: 10-installer-and-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - package.json
  - README.md
  - LICENSE
autonomous: true
requirements:
  - INF-06
  - INF-07
  - INF-08

must_haves:
  truths:
    - "package.json files array includes commands/ so npm pack distributes all framework files"
    - "README.md documents the full TSX framework including all 32 commands, architecture overview, and quick start"
    - "GSD attribution is visible in README.md, package.json, and LICENSE"
    - "npm pack --dry-run shows all 4 directories (bin/, commands/, skills/, topstepx/) in the package"
  artifacts:
    - path: "package.json"
      provides: "Updated npm package metadata with commands/ in files array and GSD attribution"
      contains: "commands/"
    - path: "README.md"
      provides: "Full framework documentation with command reference, architecture, and GSD credits"
      min_lines: 150
    - path: "LICENSE"
      provides: "MIT license with GSD attribution note"
      contains: "GSD"
  key_links:
    - from: "package.json"
      to: "commands/"
      via: "files array entry"
      pattern: "commands/"
    - from: "README.md"
      to: "bin/install.js"
      via: "installation instructions reference installer flags"
      pattern: "npx topstepx-dev"
    - from: "README.md"
      to: "/tsx:new-project"
      via: "quick start section references first command"
      pattern: "tsx:new-project"
---

<objective>
Update package.json for full framework distribution, rewrite README.md from API skill documentation to full framework documentation, and add GSD attribution to README, package.json, and LICENSE.

Purpose: Package metadata and documentation make the framework discoverable and properly credited. The files array in package.json is the critical gate -- without `commands/` listed, npm will not distribute the commands directory.
Output: Updated package.json, README.md, and LICENSE ready for npm publish.
</objective>

<execution_context>
@C:/Users/bkevi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bkevi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-installer-and-distribution/10-RESEARCH.md
@.planning/phases/10-installer-and-distribution/10-01-SUMMARY.md
@package.json
@README.md
@LICENSE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json and LICENSE with framework metadata and GSD attribution</name>
  <files>package.json, LICENSE</files>
  <action>
**package.json updates:**

1. Update `files` array to include commands/:
```json
"files": ["bin/", "commands/", "skills/", "topstepx/"]
```

2. Update `description` to reflect full framework:
```
"description": "TSX -- AI framework for building TopStepX trading bots. Extends GSD (Get Shit Done) with trading domain workflows, agents, and templates."
```

3. Add framework-related keywords (append to existing, do not remove existing ones):
   Add: `"workflow"`, `"pinescript"`, `"trading-bot"`, `"gsd"`, `"get-shit-done"`, `"framework"`, `"ai-framework"`

4. Add GSD attribution in a top-level `credits` field:
```json
"credits": {
  "framework": "Built on the GSD (Get Shit Done) framework by TACHES",
  "url": "https://github.com/gsd-build/get-shit-done"
}
```

5. Keep all existing fields unchanged: name, version, bin, author, license, repository, homepage, bugs, engines.

**LICENSE updates:**

Keep the full MIT license text intact. Add a brief attribution note after the license text (not inside it):

```
---

Framework architecture derived from GSD (Get Shit Done) by TACHES
https://github.com/gsd-build/get-shit-done
Licensed under MIT.
```

This preserves the legal validity of the MIT license while providing visible attribution.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const ok = p.files.includes('commands/') && p.credits && p.description.includes('GSD'); console.log(ok ? 'PASS' : 'FAIL: missing fields')"</automated>
  </verify>
  <done>package.json files array includes commands/, description mentions GSD, credits field exists with GSD attribution URL, and LICENSE contains GSD attribution note</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md for full framework documentation</name>
  <files>README.md</files>
  <action>
Rewrite README.md from the current API skill documentation to full framework documentation. The README should follow this structure:

**1. Title and tagline:**
```
# TSX -- TopStepX AI Framework
```
One-line description: "An AI framework that teaches coding assistants how to build, convert, and deploy TopStepX trading bots. Built on the [GSD](https://github.com/gsd-build/get-shit-done) framework."

**2. What It Does (2-3 paragraphs):**
- TSX gives AI coding assistants everything they need to build TopStepX trading bots: domain knowledge, guided workflows, safety guardrails, and cross-language conversion
- Works with Claude Code, OpenCode, Codex CLI, and Gemini CLI
- Extends the GSD (Get Shit Done) framework with trading-specific agents, workflows, and templates
- Mention key capabilities: new project from scratch, PineScript conversion, language adaptation, full project lifecycle (plan, execute, verify)

**3. Installation:**
Keep the same install commands but add --uninstall:
```bash
npx topstepx-dev                              # Interactive
npx topstepx-dev --claude --global             # Claude Code, global
npx topstepx-dev --all --global                # All platforms
npx topstepx-dev --uninstall --claude --global  # Uninstall
```

Flags table: keep existing + add --uninstall flag.

**4. Quick Start:**
```
1. Install: npx topstepx-dev --claude --global
2. Open a project in Claude Code
3. Run: /tsx:new-project
4. Follow the guided conversation to define your trading bot
5. TSX handles research, planning, implementation, and verification
```

**5. Command Reference:**
Group all 32 commands by category in tables:

| Command | Description |
|---------|-------------|
Core Workflow (8): new-project, discuss-phase, plan-phase, execute-phase, verify-work, audit-milestone, complete-milestone, new-milestone
Navigation (4): progress, resume-work, pause-work, help
Phase Management (5): add-phase, insert-phase, remove-phase, list-phase-assumptions, plan-milestone-gaps
TSX-Specific (2): adapt-language, adapt-pinescript
Utilities (13): map-codebase, quick, debug, add-todo, check-todos, settings, set-profile, update, research-phase, validate-phase, health, cleanup, add-tests

Use the description from each command's YAML frontmatter. Get these from the actual command files at commands/tsx/*.md.

**6. Architecture Overview:**
Brief explanation of how TSX is structured:
```
commands/tsx/     -- 32 thin entry points (/tsx:* slash commands)
topstepx/
  agents/        -- 12 trading-aware AI agents (tsx-*)
  workflows/     -- Orchestration workflows (plan, execute, verify, convert)
  templates/     -- Output format templates (project docs, bot scaffolds, reports)
  references/    -- Domain knowledge (TopStepX API, PineScript, safety patterns)
  bin/           -- CLI utilities (tsx-tools.cjs)
skills/          -- API skill (auto-activates on TopStepX mention)
```

**7. Platform Support:**
Updated table showing what gets installed per platform:

| Platform | Commands | Agents | Core | Skills |
|----------|----------|--------|------|--------|
| Claude Code | commands/tsx/*.md | agents/tsx-*.md | topstepx/ | skills/topstepx-api/ |
| OpenCode | commands/tsx-*.md | agents/tsx-*.md | topstepx/ | skills/topstepx-api/ |
| Gemini CLI | commands/tsx/*.toml | agents/tsx-*.md | topstepx/ | skills/topstepx-api/ |
| Codex CLI | skills/tsx-*/SKILL.md | skills/tsx-*/SKILL.md | topstepx/ | skills/topstepx-api/ |

Include note about global vs local install paths.

**8. GSD Attribution:**
Dedicated "Built on GSD" section:
"TSX extends the [GSD (Get Shit Done)](https://github.com/gsd-build/get-shit-done) framework by TACHES. GSD provides the execution engine -- wave-based parallel planning, atomic commits, deviation handling, verification protocols. TSX adds trading domain expertise on top: TopStepX API knowledge, PineScript conversion, safety guardrails, and trading-specific workflows."

**9. License:**
"MIT" with link to LICENSE file.

**Important:** Read the actual command files to get accurate descriptions for the command reference table. Use `grep -h "^description:" commands/tsx/*.md` to extract them.
  </action>
  <verify>
    <automated>node -e "const r = require('fs').readFileSync('README.md','utf8'); const checks = [r.includes('tsx:new-project'), r.includes('GSD'), r.includes('--uninstall'), r.includes('adapt-pinescript'), r.length > 3000]; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: ' + checks.join(','))"</automated>
  </verify>
  <done>README.md documents the full TSX framework with all 32 commands listed, architecture overview, installation with --uninstall flag, quick start guide, platform support table, and visible GSD attribution section</done>
</task>

</tasks>

<verification>
1. `npm pack --dry-run` shows bin/, commands/, skills/, topstepx/ all included in the package
2. `node -e "const p = require('./package.json'); console.log(p.files)"` shows all 4 directories
3. README.md contains all 32 command names
4. README.md contains GSD attribution with link to https://github.com/gsd-build/get-shit-done
5. LICENSE contains GSD attribution note after the MIT text
6. `grep -c "tsx:" README.md` returns 32+ (all commands referenced)
</verification>

<success_criteria>
- package.json files array includes commands/ for proper npm distribution
- package.json description and credits reference GSD
- README.md is a complete framework reference document (not just API skill docs)
- README.md has command reference covering all 32 /tsx:* commands
- README.md has architecture overview, platform support, and quick start
- LICENSE has GSD attribution appended after the MIT license text
- npm pack --dry-run confirms all directories will be included in the published package
</success_criteria>

<output>
After completion, create `.planning/phases/10-installer-and-distribution/10-02-SUMMARY.md`
</output>
