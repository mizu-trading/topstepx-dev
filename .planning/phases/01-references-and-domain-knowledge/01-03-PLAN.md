---
phase: 01-references-and-domain-knowledge
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - topstepx/references/checkpoints.md
  - topstepx/references/verification-patterns.md
autonomous: true

must_haves:
  truths:
    - "An agent loading checkpoints.md gets the complete checkpoint system adapted for trading workflows with tsx-* naming"
    - "An agent loading verification-patterns.md gets both GSD verification patterns and trading-specific verification patterns for order placement, WebSocket connections, and enum usage"
    - "Neither file contains gsd-* naming (except in explicit GSD attribution lines)"
  artifacts:
    - path: "topstepx/references/checkpoints.md"
      provides: "TSX-adapted checkpoint reference with trading checkpoint examples"
      contains: "tsx-"
    - path: "topstepx/references/verification-patterns.md"
      provides: "TSX-adapted verification patterns with trading verification additions"
      contains: "tsx-"
  key_links:
    - from: "topstepx/references/verification-patterns.md"
      to: "topstepx/references/safety-patterns.md"
      via: "Trading verification patterns reference safety guardrails"
      pattern: "safety-patterns"
    - from: "topstepx/references/checkpoints.md"
      to: "topstepx/references/verification-patterns.md"
      via: "Checkpoints reference verification patterns for what to verify"
      pattern: "verification-patterns"
---

<objective>
Adapt the two largest GSD reference files (checkpoints.md at 39KB and verification-patterns.md at 17KB) for the TSX trading domain. These require medium-to-heavy adaptation because they need substantive trading-specific content additions beyond simple renaming.

Purpose: These two files are the most content-heavy GSD references. checkpoints.md defines the checkpoint system that trading workflows use for human-verify gates (e.g., "verify the bot connects to WebSocket and receives quotes"). verification-patterns.md defines how to verify built features, and trading adds unique verification needs (order placement succeeds, enums are named constants, WebSocket streams data, risk guardrails are enforced).

Output: Two adapted reference files in topstepx/references/ completing REF-04.
</objective>

<execution_context>
@C:\Users\bkevi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bkevi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-references-and-domain-knowledge/01-RESEARCH.md
@C:\Users\bkevi\.claude\get-shit-done\references\checkpoints.md
@C:\Users\bkevi\.claude\get-shit-done\references\verification-patterns.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt checkpoints.md for TSX trading domain</name>
  <files>topstepx/references/checkpoints.md</files>
  <action>
    Copy `C:\Users\bkevi\.claude\get-shit-done\references\checkpoints.md` (39KB) into `topstepx/references/checkpoints.md` and apply both mechanical and substantive adaptations.

    **Mechanical adaptations (apply throughout the entire file):**
    1. Replace ALL `gsd-` with `tsx-` (agent names, file references)
    2. Replace ALL `gsd_` with `tsx_` (variable names)
    3. Replace ALL `/gsd:` with `/tsx:` (command references)
    4. Replace "GSD" with "TSX" where it refers to the framework (not where GSD is credited)
    5. Replace `get-shit-done` path references with `topstepx` for framework content

    **Substantive additions (append to existing sections, do NOT reorganize):**

    Add a "Trading Checkpoint Examples" section (or subsection) that includes:

    1. **API Connection Checkpoint** -- Verify the bot authenticates and receives a valid JWT:
       ```
       checkpoint:human-verify
       what-built: Bot authenticates with TopStepX API
       how-to-verify:
         1. Run the bot with valid API credentials
         2. Check console output for "Authenticated successfully"
         3. Verify token refresh timer is set (logged at startup)
       resume-signal: Type "approved" or describe issues
       ```

    2. **WebSocket Streaming Checkpoint** -- Verify real-time data flows:
       ```
       checkpoint:human-verify
       what-built: Bot connects to WebSocket and receives market data
       how-to-verify:
         1. Run the bot with WebSocket connection enabled
         2. Verify quote updates appear in console (bid/ask/last for target instrument)
         3. Verify reconnection works (disconnect network briefly, watch for auto-reconnect)
       resume-signal: Type "approved" or describe issues
       ```

    3. **Order Placement Checkpoint** -- Verify orders reach the exchange:
       ```
       checkpoint:human-verify
       what-built: Bot can place and cancel orders
       how-to-verify:
         1. Run the bot in simulation/evaluation account
         2. Trigger a test order (use smallest position size)
         3. Verify order appears in account's open orders
         4. Verify bracket orders (TP + SL) are attached
         5. Cancel the test order
       resume-signal: Type "approved" or describe issues
       ```

    4. **Risk Guardrails Checkpoint** -- Verify safety limits enforce:
       ```
       checkpoint:human-verify
       what-built: Risk guardrails prevent excessive exposure
       how-to-verify:
         1. Attempt to exceed max position size -- verify rejection
         2. Verify daily loss limit is tracked
         3. Verify all orders use named enum constants (check code, not runtime)
       resume-signal: Type "approved" or describe issues
       ```

    Preserve ALL existing GSD checkpoint content. These additions go at the end or in a clearly marked "Trading-Specific Checkpoints" section.
  </action>
  <verify>
    - `test -f topstepx/references/checkpoints.md` (file exists)
    - `wc -l topstepx/references/checkpoints.md` (should be >= 800 lines, larger than GSD's ~850 due to additions)
    - `grep -c "tsx-" topstepx/references/checkpoints.md` (>= 5, tsx naming present)
    - `grep -c "gsd-" topstepx/references/checkpoints.md` (should be 0, or only in explicit attribution)
    - `grep -c "WebSocket\|websocket" topstepx/references/checkpoints.md` (>= 1, trading checkpoint)
    - `grep -c "bracket\|Bracket" topstepx/references/checkpoints.md` (>= 1, order checkpoint mentions brackets)
    - `grep -c "Risk Guardrail\|risk guardrail" topstepx/references/checkpoints.md` (>= 1, risk checkpoint)
    - `grep -c "checkpoint:human-verify" topstepx/references/checkpoints.md` (>= 4, at least the 4 trading checkpoints)
  </verify>
  <done>
    checkpoints.md exists at topstepx/references/ with complete tsx-* naming, all original GSD checkpoint content preserved, and four trading-specific checkpoint examples added (API connection, WebSocket streaming, order placement with brackets, risk guardrails).
  </done>
</task>

<task type="auto">
  <name>Task 2: Adapt verification-patterns.md for TSX trading domain</name>
  <files>topstepx/references/verification-patterns.md</files>
  <action>
    Copy `C:\Users\bkevi\.claude\get-shit-done\references\verification-patterns.md` (17KB) into `topstepx/references/verification-patterns.md` and apply both mechanical and substantive adaptations.

    **Mechanical adaptations (apply throughout the entire file):**
    1. Replace ALL `gsd-` with `tsx-` (agent names, file references)
    2. Replace ALL `gsd_` with `tsx_` (variable names)
    3. Replace ALL `/gsd:` with `/tsx:` (command references)
    4. Replace "GSD" with "TSX" where it refers to the framework (not where GSD is credited)
    5. Replace `get-shit-done` path references with `topstepx` for framework content

    **Substantive additions (append to existing sections, do NOT reorganize):**

    Add a "Trading-Specific Verification Patterns" section that includes:

    1. **Order Placement Verification:**
       - Verify all order objects use named enum constants (OrderSide.Bid, not 0)
       - Verify bracket orders are attached by default (take-profit + stop-loss)
       - Verify position sizing respects max limits
       - Pattern: `grep -r "side: [0-9]" src/` should return 0 results (no bare integers)
       - Pattern: `grep -r "type: [0-9]" src/` should return 0 results

    2. **WebSocket Connection Verification:**
       - Verify .withAutomaticReconnect() is used on HubConnectionBuilder
       - Verify all hub subscriptions are re-established after reconnection
       - Verify the connection handles auth token injection
       - Pattern: `grep "withAutomaticReconnect" src/` should return >= 1 result

    3. **JWT Token Management Verification:**
       - Verify proactive refresh timer exists (not reactive on 401)
       - Verify refresh interval is < 24 hours (the token TTL)
       - Verify fallback to re-authentication if refresh fails
       - Pattern: `grep "refreshInterval\|refresh_interval\|REFRESH" src/` should return >= 1

    4. **Rate Limit Compliance Verification:**
       - Verify rate limiter wraps history endpoint calls
       - Verify rate limit constants match documented values (50/30s history, 200/60s general)
       - Pattern: `grep "50.*30\|RATE_LIMIT" src/` should return >= 1

    5. **Enum Usage Verification:**
       - Verify all 6 enum types are defined as named constants
       - Verify zero bare integer usage for enum values in API calls
       - Automated check: `grep -rn "side:\s*[0-9]" src/ | wc -l` should be 0
       - Automated check: `grep -rn "type:\s*[0-9]" src/ | wc -l` should be 0 (for order type)
       - Reference: safety-patterns.md SAF-01 for complete enum definitions

    6. **PineScript Conversion Verification (when applicable):**
       - Verify repainting audit was performed (SAF-04 checklist)
       - Verify barstate.isconfirmed equivalent is used for signal confirmation
       - Verify request.security() references use [1] offset + lookahead_on
       - Reference: safety-patterns.md SAF-04 for complete checklist

    Each verification pattern should include:
    - What to check (human-readable description)
    - How to check (grep pattern or test command)
    - Why it matters (what goes wrong if this fails)

    Preserve ALL existing GSD verification pattern content. These additions go at the end or in a clearly marked "Trading-Specific Patterns" section.
  </action>
  <verify>
    - `test -f topstepx/references/verification-patterns.md` (file exists)
    - `wc -l topstepx/references/verification-patterns.md` (should be >= 400 lines, larger than GSD's ~380 due to additions)
    - `grep -c "tsx-" topstepx/references/verification-patterns.md` (>= 5, tsx naming present)
    - `grep -c "gsd-" topstepx/references/verification-patterns.md` (should be 0, or only in attribution)
    - `grep -c "Order Placement\|order placement" topstepx/references/verification-patterns.md` (>= 1)
    - `grep -c "WebSocket\|websocket" topstepx/references/verification-patterns.md` (>= 1)
    - `grep -c "JWT\|token" topstepx/references/verification-patterns.md` (>= 1)
    - `grep -c "Rate Limit\|rate limit" topstepx/references/verification-patterns.md` (>= 1)
    - `grep -c "enum\|Enum" topstepx/references/verification-patterns.md` (>= 1)
    - `grep -c "safety-patterns" topstepx/references/verification-patterns.md` (>= 1, cross-reference)
    - `grep -c "repainting\|Repainting" topstepx/references/verification-patterns.md` (>= 1, PineScript verification)
  </verify>
  <done>
    verification-patterns.md exists at topstepx/references/ with complete tsx-* naming, all original GSD verification pattern content preserved, and six trading-specific verification patterns added (order placement with enum checks, WebSocket connection, JWT management, rate limit compliance, enum usage with grep patterns, PineScript conversion verification). Each trading pattern includes what/how/why and references safety-patterns.md where applicable.
  </done>
</task>

</tasks>

<verification>
Run all verification commands from both tasks. Additionally:
- `ls -la topstepx/references/checkpoints.md topstepx/references/verification-patterns.md` shows both files exist
- `grep -c "gsd-" topstepx/references/checkpoints.md topstepx/references/verification-patterns.md` returns 0 for both (or only attribution lines)
- Combined with Plans 01 and 02, `ls topstepx/references/ | wc -l` should show 12 files total (TOPSTEPX_API.md, PINESCRIPT.md, safety-patterns.md, questioning.md, checkpoints.md, verification-patterns.md, git-integration.md, model-profiles.md, continuation-format.md, planning-config.md, tdd.md, ui-brand.md)
</verification>

<success_criteria>
1. checkpoints.md exists with tsx-* naming, all GSD content preserved, and 4 trading checkpoint examples (API auth, WebSocket, orders, risk)
2. verification-patterns.md exists with tsx-* naming, all GSD content preserved, and 6 trading verification patterns (orders, WebSocket, JWT, rate limits, enums, PineScript)
3. Both files cross-reference safety-patterns.md where applicable
4. Zero gsd-* naming in either file (except explicit GSD attribution)
5. REF-04 is now complete across Plans 02 and 03 (all 9 GSD references adapted)
</success_criteria>

<output>
After completion, create `.planning/phases/01-references-and-domain-knowledge/01-03-SUMMARY.md`
</output>
