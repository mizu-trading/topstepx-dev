---
phase: 01-references-and-domain-knowledge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - topstepx/references/TOPSTEPX_API.md
  - topstepx/references/PINESCRIPT.md
  - topstepx/references/safety-patterns.md
autonomous: true

must_haves:
  truths:
    - "An agent loading TOPSTEPX_API.md gets REST endpoints, WebSocket events, enum definitions, rate limits, and authentication in one file"
    - "An agent loading PINESCRIPT.md gets v6 syntax, execution model, strategy/TA functions, conversion mappings, and a repainting detection cross-reference"
    - "An agent loading safety-patterns.md gets all five safety domains (enum constants, JWT refresh, rate limits, repainting audit, error handling) with copy-pasteable code"
  artifacts:
    - path: "topstepx/references/TOPSTEPX_API.md"
      provides: "Consolidated TopStepX API reference"
      contains: "Rate Limits"
    - path: "topstepx/references/PINESCRIPT.md"
      provides: "PineScript v6 reference with conversion mappings"
      contains: "barstate.isconfirmed"
    - path: "topstepx/references/safety-patterns.md"
      provides: "All SAF-01 through SAF-05 patterns"
      contains: "SAF-01"
  key_links:
    - from: "topstepx/references/PINESCRIPT.md"
      to: "topstepx/references/safety-patterns.md"
      via: "Cross-reference to SAF-04 repainting audit"
      pattern: "safety-patterns.md"
    - from: "topstepx/references/safety-patterns.md"
      to: "topstepx/references/TOPSTEPX_API.md"
      via: "References API endpoints for JWT, rate limits, enums"
      pattern: "TOPSTEPX_API.md"
---

<objective>
Create the three core trading domain reference files that form the knowledge foundation for the entire TSX framework: the consolidated API reference, the PineScript language reference, and the safety patterns reference.

Purpose: These three files are the primary context that every TSX agent will load before generating trading bot code. They must be complete, accurate, and agent-optimized (concise enough for context windows, detailed enough for code generation).

Output: Three substantive Markdown files in topstepx/references/ covering REF-01, REF-02, SAF-01 through SAF-05.
</objective>

<execution_context>
@C:\Users\bkevi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bkevi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-references-and-domain-knowledge/01-RESEARCH.md
@TOPSTEPX_API.md
@PINESCRIPT.md
@skills/topstepx-api/references/enums.md
@skills/topstepx-api/references/rest-api.md
@skills/topstepx-api/references/realtime.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TOPSTEPX_API.md and PINESCRIPT.md in topstepx/references/</name>
  <files>topstepx/references/TOPSTEPX_API.md, topstepx/references/PINESCRIPT.md</files>
  <action>
    Create the directory `topstepx/references/` (it does not exist yet).

    **TOPSTEPX_API.md (REF-01):**
    Copy the existing `TOPSTEPX_API.md` from the repo root into `topstepx/references/TOPSTEPX_API.md`. The existing file (1095 lines, 24KB) is already a complete consolidated reference covering REST, WebSocket, and enums. Make these enhancements:

    1. Ensure the **Rate Limits** section is prominent (near the top, not buried). It must contain a structured table with these exact values:
       - History endpoints (retrieveBars): 50 requests per 30 seconds
       - General endpoints: 200 requests per 60 seconds
    2. Ensure the **Authentication** section prominently states: "Session tokens are valid for 24 hours. See safety-patterns.md SAF-02 for the proactive refresh pattern."
    3. Ensure the **Enum Definitions** section includes a warning: "ALWAYS use named enum constants, NEVER bare integers. See safety-patterns.md SAF-01."
    4. Add a brief "Safety Cross-References" section at the end pointing to safety-patterns.md for SAF-01 (enums), SAF-02 (JWT), SAF-03 (rate limits), SAF-05 (error handling).

    Do NOT restructure the existing content. Enhance it in-place.

    **PINESCRIPT.md (REF-02):**
    Copy the existing `PINESCRIPT.md` from the repo root into `topstepx/references/PINESCRIPT.md`. The existing file (431 lines, 14KB) covers v6 syntax, execution model, strategy/TA functions, and conversion mappings. Make these enhancements:

    1. Add a "Repainting Detection" section (or expand if one exists) that cross-references safety-patterns.md SAF-04. Include a brief summary of the four repainting checks:
       - request.security() without [1] offset + lookahead_on
       - Conditions on unconfirmed bars (need barstate.isconfirmed)
       - Realtime-only logic (barstate.isrealtime)
       - Fluid values in conditions (high/low/close without [1])
    2. State the default policy: "Converted bots should use bar-close signals (barstate.isconfirmed) unless the user explicitly requests tick-based execution."
    3. Ensure the conversion mapping tables include mappings for: strategy.entry -> order placement, ta.* -> trading-signals/pandas-ta equivalents, alertcondition -> event handlers.
    4. Add a "Safety Cross-References" section pointing to safety-patterns.md SAF-04.

    Do NOT restructure the existing content. Enhance it in-place.
  </action>
  <verify>
    Verify both files exist and are substantive:
    - `test -f topstepx/references/TOPSTEPX_API.md && wc -l topstepx/references/TOPSTEPX_API.md` (should be >= 1100 lines)
    - `test -f topstepx/references/PINESCRIPT.md && wc -l topstepx/references/PINESCRIPT.md` (should be >= 440 lines)
    - `grep -c "Rate Limits" topstepx/references/TOPSTEPX_API.md` (should be >= 1)
    - `grep -c "50.*30" topstepx/references/TOPSTEPX_API.md` (rate limit constant present)
    - `grep -c "NEVER bare integers" topstepx/references/TOPSTEPX_API.md` (enum warning present)
    - `grep -c "barstate.isconfirmed" topstepx/references/PINESCRIPT.md` (repainting content present)
    - `grep -c "request.security" topstepx/references/PINESCRIPT.md` (repainting content present)
    - `grep -c "safety-patterns.md" topstepx/references/TOPSTEPX_API.md` (cross-reference present)
    - `grep -c "safety-patterns.md" topstepx/references/PINESCRIPT.md` (cross-reference present)
  </verify>
  <done>
    TOPSTEPX_API.md exists at topstepx/references/ with REST, WebSocket, enum docs, prominent rate limits table, 24hr JWT expiry note, enum constant warning, and safety cross-references. PINESCRIPT.md exists at topstepx/references/ with v6 syntax, conversion mappings, repainting detection section, bar-close default policy, and safety cross-reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create safety-patterns.md consolidating SAF-01 through SAF-05</name>
  <files>topstepx/references/safety-patterns.md</files>
  <action>
    Create `topstepx/references/safety-patterns.md` as the consolidated safety reference for the entire TSX framework. This file is loaded by agents and templates to embed safety guardrails in all generated trading bot code.

    Structure the file with these exact sections:

    **Header:** Title, purpose statement ("This reference contains mandatory safety patterns for all TopStepX trading bot code. Agents and templates MUST load this file and apply these patterns."), and a table of contents linking to each SAF section.

    **SAF-01: Enum Constants (Risk Guardrails)**
    - Rule: "ALWAYS use named enum constants, NEVER bare integers for API enums"
    - Include complete enum constant definitions for ALL enums documented in TOPSTEPX_API.md:
      - OrderSide: { Bid: 0, Ask: 1 }
      - OrderType: { Unknown: 0, Limit: 1, Market: 2, StopLimit: 3, Stop: 4, TrailingStop: 5, JoinBid: 6, JoinAsk: 7 }
      - OrderStatus: { None: 0, Open: 1, Filled: 2, Cancelled: 3, Expired: 4, Rejected: 5, Pending: 6 }
      - PositionType: { Undefined: 0, Long: 1, Short: 2 }
      - TimeInForce: { Day: 0, GTC: 1, GTD: 2, IOC: 3, FOK: 4 }
      - BarType / BarTimeUnit as documented
    - Include CORRECT vs WRONG code example (use named constant vs bare integer)
    - Include risk guardrail patterns: position sizing limits, max loss checks, bracket order defaults
    - State that bracket orders (take-profit + stop-loss) should be the DEFAULT for all order placements

    **SAF-02: JWT Token Refresh**
    - Rule: "Proactive refresh before expiry, never reactive on 401"
    - State: "Session tokens are valid for 24 hours. Refresh at 23 hours (1 hour before expiry)."
    - Include the complete TokenManager class from the research (the code example with startRefreshTimer, refreshToken, the 23-hour interval)
    - Include the API endpoint: POST /api/Auth/validate with Bearer token
    - Include fallback: if refresh fails, re-authenticate from scratch using POST /api/Auth/loginKey

    **SAF-03: Rate Limit Compliance**
    - Present rate limits as a PROMINENT TABLE:
      | Endpoint Category | Limit | Window |
      |---|---|---|
      | History (retrieveBars) | 50 requests | 30 seconds |
      | General (all other) | 200 requests | 60 seconds |
    - Include the complete RateLimiter class from the research (with waitForSlot, timestamp tracking)
    - Include usage pattern showing how to wrap API calls with the rate limiter

    **SAF-04: PineScript Repainting Audit**
    - Include the complete 4-point checklist from the research:
      1. request.security() without [1] offset + lookahead_on (BOTH required, they are INTERDEPENDENT)
      2. Conditions on unconfirmed bars (need barstate.isconfirmed)
      3. Realtime-only logic (barstate.isrealtime)
      4. Fluid values in conditions (high/low/close without [1])
    - Include REPAINTS vs SAFE code examples for each check
    - State the default policy: "When in doubt, require barstate.isconfirmed for all entry/exit conditions. Converted bots should use bar-close signals unless the user explicitly requests tick-based execution."

    **SAF-05: Error Handling Patterns**
    - Include the complete placeOrderSafe function from the research
    - Cover three failure modes:
      1. Rate limited (429): back off and retry once
      2. Order rejected (!data.success): log and DO NOT retry (investigate cause)
      3. Connection failure (catch): log the error
    - Include WebSocket reconnection pattern: use .withAutomaticReconnect() on the SignalR HubConnectionBuilder, re-subscribe to all hubs after reconnection
    - Include a brief pattern for handling partial fills and position reconciliation

    Total target size: 8-12KB (250-400 lines). This is a reference document, not a tutorial. Be concise but complete.
  </action>
  <verify>
    - `test -f topstepx/references/safety-patterns.md` (file exists)
    - `wc -l topstepx/references/safety-patterns.md` (should be 250-400 lines)
    - `grep -c "SAF-01" topstepx/references/safety-patterns.md` (>= 1)
    - `grep -c "SAF-02" topstepx/references/safety-patterns.md` (>= 1)
    - `grep -c "SAF-03" topstepx/references/safety-patterns.md` (>= 1)
    - `grep -c "SAF-04" topstepx/references/safety-patterns.md` (>= 1)
    - `grep -c "SAF-05" topstepx/references/safety-patterns.md` (>= 1)
    - `grep -c "NEVER bare integers" topstepx/references/safety-patterns.md` (enum rule present)
    - `grep -c "TokenManager" topstepx/references/safety-patterns.md` (JWT pattern present)
    - `grep -c "50.*30" topstepx/references/safety-patterns.md` (rate limit constants)
    - `grep -c "barstate.isconfirmed" topstepx/references/safety-patterns.md` (repainting checklist)
    - `grep -c "placeOrderSafe" topstepx/references/safety-patterns.md` (error handling pattern)
  </verify>
  <done>
    safety-patterns.md exists at topstepx/references/ with all five SAF sections, each containing copy-pasteable code patterns, prominent constants/tables, and clear rules. The file is 250-400 lines, agent-loadable, and covers: enum constants with all 6+ enum types, JWT proactive refresh with TokenManager, rate limit table and RateLimiter class, 4-point repainting audit checklist, and error handling for 429/rejected/connection failures.
  </done>
</task>

</tasks>

<verification>
Run all verification commands from both tasks. Additionally:
- `ls -la topstepx/references/` shows exactly 3 files (TOPSTEPX_API.md, PINESCRIPT.md, safety-patterns.md)
- `grep -r "gsd-" topstepx/references/` returns 0 results (no GSD naming leaked into domain references)
- All three files have substantive content (not stubs)
</verification>

<success_criteria>
1. topstepx/references/ directory exists with TOPSTEPX_API.md, PINESCRIPT.md, and safety-patterns.md
2. TOPSTEPX_API.md is a complete API reference (>= 1100 lines) with rate limits, JWT expiry, enum warnings, and safety cross-references
3. PINESCRIPT.md is a complete PineScript reference (>= 440 lines) with repainting detection section and safety cross-reference
4. safety-patterns.md covers all five SAF domains (SAF-01 through SAF-05) with code patterns, constants, and checklists
5. Cross-references link the three documents together (API -> safety, PineScript -> safety)
</success_criteria>

<output>
After completion, create `.planning/phases/01-references-and-domain-knowledge/01-01-SUMMARY.md`
</output>
