---
phase: 04-core-execution-workflows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - topstepx/workflows/execute-phase.md
  - topstepx/workflows/execute-plan.md
  - topstepx/workflows/transition.md
autonomous: true
requirements: [WKF-04]

must_haves:
  truths:
    - "execute-phase workflow spawns tsx-executor agents with trading reference loading directives"
    - "execute-plan workflow references topstepx paths for execution context (@$HOME/.claude/topstepx/...)"
    - "transition workflow routes using /tsx: command references for next-step suggestions"
    - "All three workflows form a consistent delegation chain with tsx-* references throughout"
  artifacts:
    - path: "topstepx/workflows/execute-phase.md"
      provides: "Wave-based parallel execution orchestration"
      min_lines: 459
    - path: "topstepx/workflows/execute-plan.md"
      provides: "Per-plan execution logic with trading context"
      min_lines: 449
    - path: "topstepx/workflows/transition.md"
      provides: "Auto-advance routing between phases"
      min_lines: 544
  key_links:
    - from: "topstepx/workflows/execute-phase.md"
      to: "topstepx/workflows/execute-plan.md"
      via: "delegation (executor spawning with execute-plan as context)"
      pattern: "execute-plan\\.md"
    - from: "topstepx/workflows/execute-phase.md"
      to: "topstepx/workflows/transition.md"
      via: "auto-advance call after phase completion"
      pattern: "transition"
    - from: "topstepx/workflows/execute-phase.md"
      to: "topstepx/agents/tsx-executor.md"
      via: "subagent_type spawning"
      pattern: "tsx-executor"
---

<objective>
Adapt the execute-phase, execute-plan, and transition GSD workflows into TSX equivalents with trading-aware execution context.

Purpose: These three files form the execution engine -- the core loop that runs plans, commits tasks, and advances through phases. execute-phase orchestrates wave-based parallel execution, execute-plan handles individual plan execution, and transition handles auto-advance routing. All three must have consistent tsx-* references and topstepx paths.

Output: Three workflow files in topstepx/workflows/ -- execute-phase.md (~459+ lines), execute-plan.md (~449+ lines), transition.md (~544+ lines) -- with zero GSD references and trading reference loading in executor prompts.
</objective>

<execution_context>
@$HOME/.claude/topstepx/workflows/execute-plan.md
@$HOME/.claude/topstepx/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-execution-workflows/04-RESEARCH.md

Source files to adapt FROM:
- C:/Users/bkevi/.claude/get-shit-done/workflows/execute-phase.md (459 lines)
- C:/Users/bkevi/.claude/get-shit-done/workflows/execute-plan.md (449 lines)
- C:/Users/bkevi/.claude/get-shit-done/workflows/transition.md (544 lines)

<interfaces>
Naming transformation rules (apply to ALL occurrences):
| Find | Replace |
|------|---------|
| `gsd-executor` | `tsx-executor` |
| `gsd-planner` | `tsx-planner` |
| `gsd-phase-researcher` | `tsx-phase-researcher` |
| `gsd-plan-checker` | `tsx-plan-checker` |
| `gsd-verifier` | `tsx-verifier` |
| `gsd-debugger` | `tsx-debugger` |
| `gsd-codebase-mapper` | `tsx-codebase-mapper` |
| `gsd-roadmapper` | `tsx-roadmapper` |
| `gsd-nyquist-auditor` | `tsx-nyquist-auditor` |
| `gsd-research-synthesizer` | `tsx-research-synthesizer` |
| `gsd-integration-checker` | `tsx-integration-checker` |
| `gsd-researcher` | `tsx-researcher` |
| `get-shit-done` | `topstepx` |
| `gsd-tools.cjs` | `tsx-tools.cjs` |
| `/gsd:` | `/tsx:` |
| `GSD` | `TSX` (in banners, brand text) |
| `~/.gsd/` | `~/.tsx/` |
| `gsd/` | `tsx/` (branch prefixes) |
| `Skill(skill="gsd:` | `Skill(skill="tsx:` |
| `C:/Users/bkevi/.claude/get-shit-done/` | `$HOME/.claude/topstepx/` |

Delegation chain: execute-phase -> execute-plan -> transition. All three must reference each other via topstepx/ paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt execute-phase and execute-plan workflows with trading execution context</name>
  <files>topstepx/workflows/execute-phase.md, topstepx/workflows/execute-plan.md</files>
  <action>
Read both GSD source files:
- C:/Users/bkevi/.claude/get-shit-done/workflows/execute-phase.md (459 lines)
- C:/Users/bkevi/.claude/get-shit-done/workflows/execute-plan.md (449 lines)

Create topstepx/workflows/execute-phase.md by:
1. Apply ALL mechanical naming replacements from the interfaces table. Every occurrence.
2. Replace all hardcoded Windows paths with $HOME/.claude/topstepx/.
3. In the executor spawning prompt, ensure execution_context references point to topstepx paths:
   - @$HOME/.claude/topstepx/workflows/execute-plan.md
   - @$HOME/.claude/topstepx/templates/summary.md
   - @$HOME/.claude/topstepx/references/checkpoints.md
   - @$HOME/.claude/topstepx/references/tdd.md
4. In the verifier spawning prompt (if present in execute-phase), ensure tsx-verifier is referenced with topstepx paths.
5. Update all banner text from GSD to TSX.
6. Ensure Skill() auto-advance invocations use tsx: prefix.

Create topstepx/workflows/execute-plan.md by:
1. Apply ALL mechanical naming replacements. Every occurrence.
2. Replace all hardcoded Windows paths with $HOME/.claude/topstepx/.
3. Ensure all @file references use topstepx paths (templates/summary.md, references/checkpoints.md, etc.).
4. Update tsx-tools.cjs init calls and all CLI operations.
5. Update banner text from GSD to TSX.

After writing both, verify zero GSD references remain in each.
  </action>
  <verify>
    <automated>grep -c "gsd-\|get-shit-done\|/gsd:\|C:/Users/bkevi" topstepx/workflows/execute-phase.md topstepx/workflows/execute-plan.md | grep -v ":0$" | wc -l | grep -q "^0$" && grep -c "tsx-executor" topstepx/workflows/execute-phase.md | grep -qv "^0$" && grep -c "topstepx" topstepx/workflows/execute-plan.md | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>execute-phase.md and execute-plan.md exist with zero GSD references, tsx-executor spawning, and all topstepx path references for execution context</done>
</task>

<task type="auto">
  <name>Task 2: Adapt transition workflow with tsx command routing</name>
  <files>topstepx/workflows/transition.md</files>
  <action>
Read the GSD source at C:/Users/bkevi/.claude/get-shit-done/workflows/transition.md (544 lines). Create topstepx/workflows/transition.md by:

1. Apply ALL mechanical naming replacements from the interfaces table. Every occurrence.
2. Replace all hardcoded Windows paths with $HOME/.claude/topstepx/.
3. Update all next-step command routing from /gsd: to /tsx: (execute-phase, plan-phase, discuss-phase, verify-work, etc.).
4. Update all Skill() invocations to use tsx: prefix.
5. Update tsx-tools.cjs calls for state management operations.
6. Update banner text from GSD to TSX.

The transition workflow is the auto-advance router that determines what happens after a phase completes. All command references must use /tsx: prefix so it routes to TSX workflows, not GSD.

After writing, verify zero GSD references remain.
  </action>
  <verify>
    <automated>grep -c "gsd-\|get-shit-done\|/gsd:\|C:/Users/bkevi" topstepx/workflows/transition.md | grep -q "^0$" && grep -c "/tsx:" topstepx/workflows/transition.md | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>transition.md exists with zero GSD references, /tsx: command routing throughout, and tsx-tools.cjs calls for state management</done>
</task>

</tasks>

<verification>
All three files exist and pass:
1. `grep -rn "gsd-\|get-shit-done\|/gsd:\|C:/Users/bkevi" topstepx/workflows/execute-phase.md topstepx/workflows/execute-plan.md topstepx/workflows/transition.md | wc -l` returns 0
2. `grep -c "tsx-executor" topstepx/workflows/execute-phase.md` returns > 0
3. `grep -c "topstepx" topstepx/workflows/execute-plan.md` returns > 0
4. `grep -c "/tsx:" topstepx/workflows/transition.md` returns > 0
5. Delegation chain consistent: execute-phase references execute-plan.md via topstepx path
</verification>

<success_criteria>
- execute-phase.md, execute-plan.md, and transition.md exist in topstepx/workflows/
- Zero GSD references remain in all three files
- execute-phase spawns tsx-executor with topstepx execution context paths
- execute-plan references topstepx templates and references
- transition routes using /tsx: commands and Skill(skill="tsx:...") invocations
- Delegation chain is consistent (execute-phase -> execute-plan -> transition all use topstepx paths)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-execution-workflows/04-02-SUMMARY.md`
</output>
