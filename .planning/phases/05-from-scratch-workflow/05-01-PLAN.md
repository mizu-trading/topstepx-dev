---
phase: 05-from-scratch-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - topstepx/workflows/new-project.md
autonomous: true
requirements:
  - WKF-01

must_haves:
  truths:
    - "The workflow asks trading-specific opening question (trading idea, not generic 'what do you want to build')"
    - "The workflow references questioning.md's 5 trading domains during follow-up questioning"
    - "The workflow generates strategy-spec.md and risk-parameters.md during Step 4 (alongside PROJECT.md)"
    - "Risk parameters are captured during questioning (Step 3) and generated in Step 4, NOT deferred to Step 7"
    - "The workflow uses trading requirement categories (API-, STR-, RSK-, RTD-, SAF-, BOT-) instead of generic ones in Step 7"
    - "The workflow's research prompts (Step 6) include trading-specific context for the 4 parallel researchers"
    - "Auto-mode extracts trading-specific fields (instrument, strategy, risk) from provided documents"
    - "The workflow still routes to /tsx:discuss-phase or /tsx:plan-phase after completion (Step 9)"
  artifacts:
    - path: "topstepx/workflows/new-project.md"
      provides: "Trading-specific project initialization workflow"
      contains: "instrument|strategy type|execution model|risk tolerance|account type"
      min_lines: 1200
  key_links:
    - from: "topstepx/workflows/new-project.md (Step 3)"
      to: "topstepx/references/questioning.md"
      via: "explicit reference to 5 trading domains"
      pattern: "questioning.md"
    - from: "topstepx/workflows/new-project.md (Step 4)"
      to: "topstepx/templates/strategy-spec.md"
      via: "template reference for strategy specification generation"
      pattern: "strategy-spec"
    - from: "topstepx/workflows/new-project.md (Step 4)"
      to: "topstepx/templates/risk-parameters.md"
      via: "template reference for risk parameter generation"
      pattern: "risk-parameters"
    - from: "topstepx/workflows/new-project.md (Step 7)"
      to: "topstepx/references/safety-patterns.md"
      via: "safety defaults for unspecified risk parameters"
      pattern: "safety-patterns"
---

<objective>
Inject deep trading domain content into the existing new-project.md workflow, transforming it from a naming-only GSD adaptation into a trading-specific project initialization experience.

Purpose: The new-project workflow is the entry point for all trading bot projects. Without trading-aware questioning and artifact generation, users get generic project initialization that downstream agents cannot act on for trading-specific code generation. This is the "most leveraged moment" -- deep questioning here means better plans, better execution, better outcomes.

Output: Modified `topstepx/workflows/new-project.md` with trading-specific questioning (Step 3), strategy-spec + risk-parameters generation (Step 4), trading research prompts (Step 6), trading requirement categories (Step 7), and trading-specific completion summary (Step 9).
</objective>

<execution_context>
@C:/Users/bkevi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bkevi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-from-scratch-workflow/05-RESEARCH.md

Source file to modify:
@topstepx/workflows/new-project.md

Reference files that the workflow must reference (do NOT duplicate their content, only reference by name):
@topstepx/references/questioning.md
@topstepx/templates/strategy-spec.md
@topstepx/templates/risk-parameters.md
@topstepx/references/safety-patterns.md
@topstepx/templates/project.md

<interfaces>
<!-- The workflow already references these agents and tools -- preserve all existing references -->

From topstepx/workflows/new-project.md (existing structure):
- Step 1: tsx-tools.cjs init new-project
- Step 2: Brownfield detection
- Step 2a: Auto-mode config collection
- Step 3: Deep questioning (MODIFY)
- Step 4: Write PROJECT.md (MODIFY)
- Step 5: Workflow preferences
- Step 5.5: Resolve model profile
- Step 6: Research decision + 4 parallel tsx-project-researcher agents (MODIFY)
- Step 7: Define requirements (MODIFY)
- Step 8: Create roadmap via tsx-roadmapper agent
- Step 9: Done (MODIFY)

From topstepx/references/questioning.md (5 trading domains to reference):
1. Instrument and Market
2. Strategy Type
3. Execution Model
4. Risk Tolerance
5. Account and Environment

From topstepx/templates/strategy-spec.md (output artifact):
- Strategy Name, Type, Instrument, Timeframe, Market Hours, Execution Model
- Indicators table, Entry Rules, Exit Rules, Risk Parameters

From topstepx/templates/risk-parameters.md (output artifact):
- Account Context, Position Sizing, Loss Limits, Bracket Orders, Kill Switch Conditions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject trading content into Steps 3, 4, and auto-mode (questioning + artifact generation)</name>
  <files>topstepx/workflows/new-project.md</files>
  <action>
Modify the existing `topstepx/workflows/new-project.md` in-place. Do NOT restructure the step numbering or remove existing GSD orchestration logic. Only inject trading-specific content into the sections listed below.

**Step 3 (Deep Questioning) -- MAJOR CHANGE (~80 lines added):**

1. Replace the generic opening question "What do you want to build?" with a trading-specific opener:
   ```
   "Tell me about your trading idea -- what do you want to trade and how?"
   ```

2. Replace the generic "Follow the thread" guidance with trading-aware follow-up guidance that explicitly references the 5 trading domains from `questioning.md`:
   - Instrument and Market -- what they're trading, which contract, market hours
   - Strategy Type -- the logic: trend-following, mean-reversion, breakout, scalping
   - Execution Model -- bar-close vs tick, order types, bracket config
   - Risk Tolerance -- position sizing, max loss, kill switch (NON-OPTIONAL)
   - Account and Environment -- TopStepX eval/funded, Node.js/Python, deployment

3. Add a note: "Consult `questioning.md` for specific AskUserQuestion examples, anti-patterns, and the freeform rule."

4. Add risk parameter capture guidance: "Risk parameters (position sizing, max loss, brackets, kill switch) must be captured during this step. If the user doesn't specify, state the conservative defaults from safety-patterns.md and confirm."

5. Add PineScript detection: "If the user mentions PineScript, capture the strategy context and note it for repainting audit. Optionally suggest `/tsx:adapt-pinescript` for full conversion workflow."

6. Keep the existing "Check context" and "Decision gate" sections, but update the context checklist to use trading-specific items matching questioning.md's context_checklist.

**Step 4 (Write PROJECT.md) -- MODERATE CHANGE (~60 lines added):**

1. After the existing PROJECT.md generation, add instructions to generate TWO additional trading artifacts:
   - `.planning/strategy-spec.md` using `templates/strategy-spec.md` template, populated from questioning answers
   - `.planning/risk-parameters.md` using `templates/risk-parameters.md` template, populated from questioning answers

2. Add trading-specific PROJECT.md content guidance showing what the Requirements section should look like for trading projects (instrument, strategy signals, risk guardrails, API integration, etc.). Use the example from the RESEARCH.md "Example: Trading PROJECT.md Content" section.

3. Add the two new files to the commit command:
   ```bash
   node "$HOME/.claude/topstepx/bin/tsx-tools.cjs" commit "docs: initialize project" --files .planning/PROJECT.md .planning/strategy-spec.md .planning/risk-parameters.md
   ```

4. Add a note about risk-parameter defaults: "For any risk parameters the user didn't specify, apply conservative defaults from safety-patterns.md: max 1 contract, daily loss limit minus 20% buffer, brackets always required, kill switch on max daily loss."

**Auto-mode section (Step 2a context / Step 4 auto path) -- MINOR CHANGE (~15 lines added):**

1. In the auto-mode document parsing section (where it says "Synthesize from provided document"), add trading-specific extraction guidance:
   - Parse for instrument mentions (ES, NQ, CL, MES, etc.)
   - Parse for strategy indicators (EMA, RSI, MACD, Bollinger, etc.)
   - Parse for risk parameters (stop loss, position size, daily limit)
   - Parse for account type (evaluation, funded, simulation)
   - Parse for PineScript code blocks (if present, flag for repainting audit and apply bar-close defaults)
   - Generate strategy-spec.md and risk-parameters.md from extracted trading context

**IMPORTANT constraints:**
- Do NOT restructure the existing 9-step flow or add new step numbers
- Do NOT duplicate content from questioning.md -- reference it by name
- Do NOT remove any existing GSD orchestration logic (auto-mode, brownfield, config, etc.)
- Preserve all existing AskUserQuestion patterns and the freeform rule
- Keep the existing "Ready?" decision gate structure
  </action>
  <verify>
    <automated>cd "C:/Users/bkevi/Documents/GitHub/mizu/topstepx-skill" && echo "=== Trading questioning (expect 10+) ===" && grep -c "instrument\|strategy type\|execution model\|risk tolerance\|account type" topstepx/workflows/new-project.md && echo "=== Strategy-spec + risk-parameters (expect 5+) ===" && grep -c "strategy-spec\|risk-parameters" topstepx/workflows/new-project.md && echo "=== Risk params non-optional (expect 5+) ===" && grep -c "position sizing\|max loss\|max contracts\|daily limit\|kill switch\|bracket order" topstepx/workflows/new-project.md && echo "=== questioning.md reference (expect 2+) ===" && grep -c "questioning.md" topstepx/workflows/new-project.md && echo "=== safety-patterns reference (expect 1+) ===" && grep -c "safety-patterns" topstepx/workflows/new-project.md && echo "=== Auto-mode still supported (expect 5+) ===" && grep -c "\-\-auto" topstepx/workflows/new-project.md && echo "=== Zero GSD references (expect 0) ===" && grep -c "gsd-\|get-shit-done\|/gsd:" topstepx/workflows/new-project.md || true</automated>
  </verify>
  <done>Steps 3 and 4 contain trading-specific questioning that references questioning.md's 5 domains, generates strategy-spec.md and risk-parameters.md alongside PROJECT.md, and captures risk parameters as non-optional with conservative defaults from safety-patterns.md. Auto-mode parses trading-specific fields from provided documents. PineScript detection is present.</done>
</task>

<task type="auto">
  <name>Task 2: Inject trading content into Steps 6, 7, 9, and success criteria (research + requirements + completion)</name>
  <files>topstepx/workflows/new-project.md</files>
  <action>
Continue modifying `topstepx/workflows/new-project.md` in-place, injecting trading content into Steps 6, 7, 9, and the success_criteria section.

**Step 6 (Research Decision) -- MODERATE CHANGE (~30 lines added):**

1. Add trading-specific context to the 4 parallel researcher prompts. Each researcher's `<question>` block should include trading-specific context when the project is a trading bot:
   - Stack researcher: "What's the standard 2025 stack for [specific trading strategy on specific instrument]?" instead of generic "[domain]". Include a note: "For trading bots, include SignalR client libraries, indicator calculation libraries (trading-signals for JS, pandas-ta for Python), and HTTP client choices."
   - Features researcher: "What features do [trading bot type] bots need? Table stakes: authentication, real-time data streaming, order placement with brackets, position tracking, error recovery."
   - Architecture researcher: "How are [trading bot type] bots typically structured? Components: auth manager, data feed, strategy engine, order manager, risk manager, event loop."
   - Pitfalls researcher: "What do [trading bot type] projects commonly get wrong? Critical: missing bracket orders, bare enum integers, stale JWT tokens, rate limit violations, repainting signals."

2. Add a note after the researcher spawning: "For trading projects, research prompts should be filled with specific trading context from the strategy specification, not generic domain placeholders."

**Step 7 (Define Requirements) -- MAJOR CHANGE (~50 lines added):**

1. Add a trading-specific requirements section that replaces the generic category examples. When the project is a trading bot, use these requirement categories instead of the generic ones (Authentication, Content, Social):
   - **API Integration (API-*):** Authentication, account search, contract lookup, order placement, position management
   - **Strategy Logic (STR-*):** Indicator calculation, signal generation, entry/exit rules, bar management
   - **Risk Management (RSK-*):** Position sizing, daily loss limits, kill switch, bracket orders
   - **Real-time Data (RTD-*):** WebSocket connection, quote streaming, order events, position events
   - **Safety Infrastructure (SAF-*):** Enum constants, JWT refresh, rate limiting, error handling
   - **Bot Lifecycle (BOT-*):** Startup sequence, shutdown sequence, logging, deployment

2. Add concrete examples for each category showing what trading requirements look like (use the examples from the RESEARCH.md "Example: Trading Requirement Categories" section).

3. Add a note: "Safety requirements (SAF-*) and Risk Management (RSK-*) are NON-OPTIONAL. Even if the user doesn't mention them, include them with defaults from safety-patterns.md. These are table stakes for any trading bot."

4. For auto-mode, add: "Auto-include all SAF-* and RSK-* requirements with conservative defaults. Include API-* and RTD-* as table stakes. Include STR-* requirements derived from the strategy specification."

5. Ensure the existing requirement quality criteria section is preserved (it's good as-is).

6. Add a reference: "Load `.planning/strategy-spec.md` and `.planning/risk-parameters.md` (created in Step 4) as context for requirements generation."

**Step 9 (Done) -- MODERATE CHANGE (~20 lines added):**

1. Add strategy-spec.md and risk-parameters.md to the completion artifact table:
   ```
   | Strategy Spec  | `.planning/strategy-spec.md`  |
   | Risk Params    | `.planning/risk-parameters.md`|
   ```

2. Add trading-specific summary content: after the existing artifact table, include a brief strategy summary:
   ```
   **Strategy:** [strategy name] ([type]) on [instrument]
   **Risk:** [max position] contracts, [$max daily loss] daily limit, brackets [always/custom]
   ```

3. For interactive mode, enhance the "Next Up" section to mention that Phase 1 should set up API authentication and data streaming first (trading build order context).

**Success criteria section -- MINOR CHANGE (~10 lines added):**

1. Add trading-specific success criteria:
   - `- [ ] strategy-spec.md captures strategy type, instruments, indicators, entry/exit rules -> **committed**`
   - `- [ ] risk-parameters.md captures position sizing, max loss, brackets, kill switch -> **committed**`
   - `- [ ] Requirements include SAF-* and RSK-* categories as non-optional`
   - `- [ ] Risk parameters captured in Step 3 (questioning), not deferred to Step 7`

**IMPORTANT constraints:**
- Do NOT change Step 8 (roadmap) beyond adding a brief hint about trading build order (references -> auth -> data -> strategy -> risk -> bot lifecycle)
- Do NOT remove any existing content from Steps 6/7/9 -- add trading-specific content alongside the existing patterns
- The generic category examples in Step 7 should remain as fallback for non-trading projects, with trading categories added as the primary path for trading bots
- Preserve all existing auto-mode behavior (skip gates, auto-approve, etc.)
  </action>
  <verify>
    <automated>cd "C:/Users/bkevi/Documents/GitHub/mizu/topstepx-skill" && echo "=== Trading requirement categories (expect 5+) ===" && grep -c "API-\|STR-\|RSK-\|RTD-\|SAF-\|BOT-" topstepx/workflows/new-project.md && echo "=== Plan-phase routing (expect 3+) ===" && grep -c "tsx:discuss-phase\|/tsx:plan-phase" topstepx/workflows/new-project.md && echo "=== Total trading terms (expect 30+) ===" && grep -c "trading\|strategy\|PineScript\|risk\|instrument\|bracket" topstepx/workflows/new-project.md && echo "=== Zero GSD references (expect 0) ===" && grep -c "gsd-\|get-shit-done\|/gsd:" topstepx/workflows/new-project.md && echo "=== Auto-mode preserved (expect 5+) ===" && grep -c "\-\-auto" topstepx/workflows/new-project.md && echo "=== File line count (expect 1200+) ===" && wc -l topstepx/workflows/new-project.md || true</automated>
  </verify>
  <done>Steps 6, 7, and 9 contain trading-specific research prompts, trading requirement categories (API/STR/RSK/RTD/SAF/BOT), and a trading-enriched completion summary with strategy-spec and risk-parameters in the artifact table. Success criteria include trading-specific checks. The file has grown from ~1,111 lines to ~1,300+ lines with all trading content injected.</done>
</task>

</tasks>

<verification>
Run the full verification suite from the VALIDATION strategy:

```bash
# 1. Trading questioning content present (expect 10+)
grep -c "instrument\|strategy type\|execution model\|risk tolerance\|account type" topstepx/workflows/new-project.md

# 2. Strategy-spec and risk-parameters generation (expect 5+)
grep -c "strategy-spec\|risk-parameters" topstepx/workflows/new-project.md

# 3. Risk parameters mentioned as non-optional (expect 5+)
grep -c "position sizing\|max loss\|max contracts\|daily limit\|kill switch\|bracket order" topstepx/workflows/new-project.md

# 4. Trading requirement categories (expect 5+)
grep -c "API-\|STR-\|RSK-\|RTD-\|SAF-\|BOT-" topstepx/workflows/new-project.md

# 5. questioning.md reference (expect 2+)
grep -c "questioning.md" topstepx/workflows/new-project.md

# 6. safety-patterns.md reference (expect 1+)
grep -c "safety-patterns" topstepx/workflows/new-project.md

# 7. Plan-phase routing still works (expect 3+)
grep -c "tsx:discuss-phase\|/tsx:plan-phase" topstepx/workflows/new-project.md

# 8. Zero GSD references remain (expect 0)
grep -c "gsd-\|get-shit-done\|/gsd:" topstepx/workflows/new-project.md

# 9. Auto-mode still supported (expect 5+)
grep -c "\-\-auto" topstepx/workflows/new-project.md
```
</verification>

<success_criteria>
1. The workflow opens with a trading-specific question ("Tell me about your trading idea") instead of generic "What do you want to build?"
2. Step 3 explicitly references questioning.md's 5 trading domains (instrument, strategy, execution, risk, account)
3. Step 4 generates `.planning/strategy-spec.md` and `.planning/risk-parameters.md` alongside PROJECT.md
4. Risk parameters are captured during Step 3 questioning and generated in Step 4 (not deferred to Step 7)
5. Step 7 uses trading requirement categories (API-, STR-, RSK-, RTD-, SAF-, BOT-) with SAF/RSK as non-optional
6. Step 6 research prompts include trading-specific context for all 4 parallel researchers
7. Auto-mode extracts trading fields from provided documents and generates trading artifacts
8. Step 9 includes strategy-spec and risk-parameters in the completion artifact table
9. All verification grep commands from the VALIDATION strategy pass their expected thresholds
10. Zero GSD references (gsd-, get-shit-done, /gsd:) remain in the file
</success_criteria>

<output>
After completion, create `.planning/phases/05-from-scratch-workflow/05-01-SUMMARY.md`
</output>
