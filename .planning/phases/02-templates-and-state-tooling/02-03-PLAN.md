---
phase: 02-templates-and-state-tooling
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - topstepx/bin/tsx-tools.cjs
  - topstepx/bin/lib/core.cjs
  - topstepx/bin/lib/state.cjs
  - topstepx/bin/lib/phase.cjs
  - topstepx/bin/lib/roadmap.cjs
  - topstepx/bin/lib/verify.cjs
  - topstepx/bin/lib/config.cjs
  - topstepx/bin/lib/template.cjs
  - topstepx/bin/lib/milestone.cjs
  - topstepx/bin/lib/commands.cjs
  - topstepx/bin/lib/init.cjs
  - topstepx/bin/lib/frontmatter.cjs
autonomous: true
requirements: [INF-01]

must_haves:
  truths:
    - "tsx-tools.cjs runs without error and displays help text with tsx-tools naming"
    - "tsx-tools.cjs state load reads .planning/STATE.md correctly"
    - "MODEL_PROFILES in core.cjs uses tsx-* agent names for all 12 entries"
    - "All path references use topstepx (not get-shit-done) and tsx (not gsd) branch templates"
    - "Zero instances of gsd- prefix, get-shit-done path, or gsd_ variable in any tsx-tools file"
  artifacts:
    - path: "topstepx/bin/tsx-tools.cjs"
      provides: "CLI entry point for state management"
      min_lines: 550
    - path: "topstepx/bin/lib/core.cjs"
      provides: "Shared utilities and MODEL_PROFILES"
      contains: "tsx-planner"
    - path: "topstepx/bin/lib/state.cjs"
      provides: "STATE.md operations"
      min_lines: 700
    - path: "topstepx/bin/lib/phase.cjs"
      provides: "Phase directory operations"
      min_lines: 850
    - path: "topstepx/bin/lib/init.cjs"
      provides: "Compound init commands"
      min_lines: 650
  key_links:
    - from: "topstepx/bin/tsx-tools.cjs"
      to: "topstepx/bin/lib/core.cjs"
      via: "require('./lib/core.cjs')"
      pattern: "require.*lib/core"
    - from: "topstepx/bin/tsx-tools.cjs"
      to: "topstepx/bin/lib/state.cjs"
      via: "require('./lib/state.cjs')"
      pattern: "require.*lib/state"
    - from: "topstepx/bin/lib/core.cjs"
      to: "all lib modules"
      via: "MODEL_PROFILES and shared utilities"
      pattern: "tsx-planner.*tsx-executor.*tsx-verifier"
    - from: "topstepx/bin/lib/init.cjs"
      to: "topstepx/bin/lib/core.cjs"
      via: "model resolution using tsx-* keys"
      pattern: "tsx-"
---

<objective>
Adapt gsd-tools.cjs and all 11 lib modules into tsx-tools.cjs with tsx-* naming, topstepx path constants, and tsx-specific model profiles. This is the CLI state management utility that all TSX workflows depend on.

Purpose: Every TSX workflow (plan, execute, verify, discuss) calls tsx-tools.cjs for state management, config access, phase operations, and template filling. Without this tool, no workflow can operate. This is a copy-and-adapt of the 6,014 lines of production-proven GSD tooling code.

Output: 12 files in topstepx/bin/ (1 entry point + 11 lib modules) providing identical CLI interface to gsd-tools.cjs but with tsx-* naming throughout.
</objective>

<execution_context>
@C:/Users/bkevi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bkevi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-templates-and-state-tooling/02-RESEARCH.md

Source code to adapt:
@C:/Users/bkevi/.claude/get-shit-done/bin/gsd-tools.cjs
@C:/Users/bkevi/.claude/get-shit-done/bin/lib/

<interfaces>
<!-- Key adaptation points from research inventory -->

From gsd-tools.cjs (592 lines):
- CLI router with subcommands: state, phase, roadmap, verify, config, template, milestone, commit, init, frontmatter, slug, timestamp, todos
- require() paths: ./lib/core.cjs, ./lib/state.cjs, etc.
- Usage text references "gsd-tools"

From lib/core.cjs (492 lines):
- MODEL_PROFILES object with 12 gsd-* keyed entries
- phase_branch_template default: "gsd/phase-{phase}-{slug}"
- milestone_branch_template: "gsd/{milestone}-{slug}"
- execGit(), resolveModel(), getConfig() utilities

From lib/config.cjs (169 lines):
- ~/.gsd/brave_api_key path
- ~/.gsd/defaults.json path
- Branch template defaults using "gsd/" prefix

From lib/init.cjs (710 lines):
- Model resolution using gsd-* keys from MODEL_PROFILES
- Error messages referencing "gsd-tools"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt entry point and core modules (tsx-tools.cjs, core.cjs, config.cjs, template.cjs, frontmatter.cjs)</name>
  <files>topstepx/bin/tsx-tools.cjs, topstepx/bin/lib/core.cjs, topstepx/bin/lib/config.cjs, topstepx/bin/lib/template.cjs, topstepx/bin/lib/frontmatter.cjs</files>
  <action>
Create topstepx/bin/ and topstepx/bin/lib/ directories if they do not exist.

**tsx-tools.cjs** -- Copy from ~/.claude/get-shit-done/bin/gsd-tools.cjs (592 lines). Apply these changes:
1. Update shebang line if present (keep #!/usr/bin/env node)
2. Update all require() paths from `./lib/*.cjs` (keep same, paths are relative)
3. Replace ALL usage text: "gsd-tools" -> "tsx-tools", "GSD" -> "TSX" in help/error strings
4. Replace ALL error messages referencing "gsd" with "tsx"
5. Replace any hardcoded path references from "get-shit-done" to "topstepx"
6. Preserve the full CLI router structure and all subcommand routing exactly as-is

**lib/core.cjs** -- Copy from ~/.claude/get-shit-done/bin/lib/core.cjs (492 lines). Apply these changes:
1. **MODEL_PROFILES**: Rename ALL 12 keys from `gsd-*` to `tsx-*`:
   - gsd-planner -> tsx-planner
   - gsd-roadmapper -> tsx-roadmapper
   - gsd-executor -> tsx-executor
   - gsd-phase-researcher -> tsx-phase-researcher
   - gsd-project-researcher -> tsx-project-researcher
   - gsd-research-synthesizer -> tsx-research-synthesizer
   - gsd-debugger -> tsx-debugger
   - gsd-codebase-mapper -> tsx-codebase-mapper
   - gsd-verifier -> tsx-verifier
   - gsd-plan-checker -> tsx-plan-checker
   - gsd-integration-checker -> tsx-integration-checker
   - gsd-nyquist-auditor -> tsx-nyquist-auditor
   Keep model allocations (opus/sonnet/haiku) identical.
2. **phase_branch_template**: "gsd/phase-{phase}-{slug}" -> "tsx/phase-{phase}-{slug}"
3. **milestone_branch_template**: "gsd/{milestone}-{slug}" -> "tsx/{milestone}-{slug}"
4. Replace all error messages and string literals containing "gsd" with "tsx"
5. Replace "get-shit-done" path references with "topstepx"

**lib/config.cjs** -- Copy from ~/.claude/get-shit-done/bin/lib/config.cjs (169 lines). Apply:
1. ~/.gsd/brave_api_key -> ~/.tsx/brave_api_key (clean separation per research recommendation)
2. ~/.gsd/defaults.json -> ~/.tsx/defaults.json
3. Branch template defaults: "gsd/" -> "tsx/"
4. Error messages gsd -> tsx

**lib/template.cjs** -- Copy from ~/.claude/get-shit-done/bin/lib/template.cjs (222 lines). Apply:
1. Any template path references pointing to "get-shit-done" -> "topstepx"
2. Error messages gsd -> tsx
3. Logic unchanged -- template fill operations work the same way

**lib/frontmatter.cjs** -- Copy from ~/.claude/get-shit-done/bin/lib/frontmatter.cjs (299 lines). Apply:
1. This module is mostly format-agnostic (YAML parsing). Minimal changes expected.
2. Replace any error messages or string literals containing "gsd" with "tsx"
3. Logic unchanged

After creating all 5 files, verify:
1. tsx-tools.cjs is executable and shows help: `node topstepx/bin/tsx-tools.cjs --help`
2. No "gsd" references remain: `grep -rn "gsd" topstepx/bin/tsx-tools.cjs topstepx/bin/lib/core.cjs topstepx/bin/lib/config.cjs topstepx/bin/lib/template.cjs topstepx/bin/lib/frontmatter.cjs`
3. MODEL_PROFILES has tsx-* keys: `grep "tsx-planner" topstepx/bin/lib/core.cjs`
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-dev && node topstepx/bin/tsx-tools.cjs --help 2>&1 | head -5 && echo "---" && grep -c "tsx-planner" topstepx/bin/lib/core.cjs && echo "---" && grep -rn "gsd-\|gsd_\|get-shit-done\|/gsd:" topstepx/bin/tsx-tools.cjs topstepx/bin/lib/core.cjs topstepx/bin/lib/config.cjs topstepx/bin/lib/template.cjs topstepx/bin/lib/frontmatter.cjs 2>&1; echo "GSD leak check exit:$?"</automated>
  </verify>
  <done>tsx-tools.cjs runs and displays TSX-branded help. core.cjs MODEL_PROFILES has all 12 tsx-* keys. config.cjs uses ~/.tsx/ paths. All 5 files have zero GSD naming leaks (excluding any intentional GSD credit comments).</done>
</task>

<task type="auto">
  <name>Task 2: Adapt remaining 7 operational lib modules</name>
  <files>topstepx/bin/lib/state.cjs, topstepx/bin/lib/phase.cjs, topstepx/bin/lib/roadmap.cjs, topstepx/bin/lib/verify.cjs, topstepx/bin/lib/milestone.cjs, topstepx/bin/lib/commands.cjs, topstepx/bin/lib/init.cjs</files>
  <action>
For each of the 7 remaining lib modules, copy from ~/.claude/get-shit-done/bin/lib/ and apply naming substitutions. These modules are primarily logic with string literal changes:

**lib/state.cjs** (721 lines) -- STATE.md operations:
- Error messages: "gsd" -> "tsx"
- Any path references: "get-shit-done" -> "topstepx"
- Logic completely unchanged (read/write/patch STATE.md)

**lib/phase.cjs** (901 lines) -- Phase directory operations:
- Error messages: "gsd" -> "tsx"
- Any path references: "get-shit-done" -> "topstepx"
- Logic completely unchanged (phase find, create, archive, decimal phases)

**lib/roadmap.cjs** (298 lines) -- ROADMAP.md operations:
- Error messages: "gsd" -> "tsx"
- Logic unchanged

**lib/verify.cjs** (820 lines) -- Verification suite:
- Error messages: "gsd" -> "tsx"
- Any agent name references: "gsd-*" -> "tsx-*" (verification may reference agent names)
- Logic unchanged

**lib/milestone.cjs** (241 lines) -- Milestone operations:
- Error messages: "gsd" -> "tsx"
- Logic unchanged

**lib/commands.cjs** (548 lines) -- Misc commands (slug, timestamp, todos, etc.):
- Error messages and usage text: "gsd" -> "tsx", "gsd-tools" -> "tsx-tools"
- Any command references: "/gsd:" -> "/tsx:"
- Logic unchanged

**lib/init.cjs** (710 lines) -- Compound init commands:
- Model resolution keys: must use "tsx-*" keys matching core.cjs MODEL_PROFILES
- Error messages: "gsd" -> "tsx"
- Any path references: "get-shit-done" -> "topstepx"
- This module is critical -- it initializes workflow contexts and resolves models. All gsd-* model lookups must become tsx-*.

**Key constraint:** This is adaptation, not refactoring. Do NOT restructure, optimize, or add features. The 6,014 lines of GSD code are production-proven. Change naming only.

After creating all 7 files, verify:
1. Full smoke test: `node topstepx/bin/tsx-tools.cjs state load` (may fail on missing STATE.md in cwd, but should not crash with module errors)
2. Zero GSD naming leaks across ALL 12 files: `grep -rn "gsd-\|gsd_\|get-shit-done\|/gsd:" topstepx/bin/`
3. All lib modules are requireable: tsx-tools.cjs can load all of them without errors
  </action>
  <verify>
    <automated>cd C:/Users/bkevi/Documents/GitHub/mizu/topstepx-dev && node -e "require('./topstepx/bin/lib/state.cjs'); require('./topstepx/bin/lib/phase.cjs'); require('./topstepx/bin/lib/roadmap.cjs'); require('./topstepx/bin/lib/verify.cjs'); require('./topstepx/bin/lib/milestone.cjs'); require('./topstepx/bin/lib/commands.cjs'); require('./topstepx/bin/lib/init.cjs'); console.log('All 7 modules loaded OK')" 2>&1 && echo "---" && grep -rl "gsd-\|gsd_\|get-shit-done\|/gsd:" topstepx/bin/ 2>&1; echo "GSD leak check exit:$?"</automated>
  </verify>
  <done>All 7 operational lib modules exist, load without errors, and have zero GSD naming leaks. tsx-tools.cjs can require all modules. Full tool chain (entry point + 11 lib modules = 12 files) is adapted and functional.</done>
</task>

</tasks>

<verification>
Full phase gate verification (run after Plan 03 completes -- this is the final plan in Phase 2):

1. Total template file count: `find topstepx/templates/ -type f | wc -l` should equal 43 (24 top-level from Plan 01 + 12 subdirectory from Plan 02 Task 1 + 7 trading-specific from Plan 02 Task 2)
2. Total tsx-tools file count: `find topstepx/bin/ -type f | wc -l` should equal 12
3. Zero GSD naming across entire topstepx/: `grep -rl "gsd-\|/gsd:\|get-shit-done" topstepx/templates/ topstepx/bin/` returns empty
4. tsx-tools.cjs smoke test: `node topstepx/bin/tsx-tools.cjs --help` shows TSX-branded help text
5. Safety embedding: `grep -c "OrderSide" topstepx/templates/bot-scaffold-js.md` > 0
6. MODEL_PROFILES: `grep -c "tsx-" topstepx/bin/lib/core.cjs` shows multiple tsx-* references
</verification>

<success_criteria>
- tsx-tools.cjs entry point runs and displays TSX help
- All 11 lib modules load without require errors
- MODEL_PROFILES has 12 tsx-* keyed entries (not gsd-*)
- config.cjs uses ~/.tsx/ for user defaults (clean separation from GSD)
- Branch templates use tsx/ prefix
- init.cjs resolves models using tsx-* keys
- Zero instances of gsd-, /gsd:, get-shit-done, or gsd_ in any file under topstepx/bin/
</success_criteria>

<output>
After completion, create `.planning/phases/02-templates-and-state-tooling/02-03-SUMMARY.md`
</output>
