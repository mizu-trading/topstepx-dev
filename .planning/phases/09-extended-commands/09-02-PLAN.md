---
phase: 09-extended-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/tsx/map-codebase.md
  - commands/tsx/quick.md
  - commands/tsx/add-todo.md
  - commands/tsx/check-todos.md
  - commands/tsx/settings.md
  - commands/tsx/set-profile.md
  - commands/tsx/update.md
  - commands/tsx/validate-phase.md
  - commands/tsx/health.md
  - commands/tsx/cleanup.md
  - commands/tsx/add-tests.md
autonomous: true
requirements: [CMD-18, CMD-19, CMD-21, CMD-22, CMD-23, CMD-24, CMD-25, CMD-27, CMD-28, CMD-29, CMD-30]

must_haves:
  truths:
    - "All 11 utility commands exist as /tsx:* slash commands in commands/tsx/"
    - "Zero GSD naming leaks across all 11 command files"
    - "All 11 commands reference correct topstepx/workflows/ files"
    - "add-tests.md has argument-instructions frontmatter field preserved"
    - "cleanup.md has no allowed-tools field (intentionally minimal)"
  artifacts:
    - path: "commands/tsx/map-codebase.md"
      provides: "/tsx:map-codebase command"
      contains: "name: tsx:map-codebase"
    - path: "commands/tsx/quick.md"
      provides: "/tsx:quick command"
      contains: "name: tsx:quick"
    - path: "commands/tsx/add-todo.md"
      provides: "/tsx:add-todo command"
      contains: "name: tsx:add-todo"
    - path: "commands/tsx/check-todos.md"
      provides: "/tsx:check-todos command"
      contains: "name: tsx:check-todos"
    - path: "commands/tsx/settings.md"
      provides: "/tsx:settings command"
      contains: "name: tsx:settings"
    - path: "commands/tsx/set-profile.md"
      provides: "/tsx:set-profile command"
      contains: "name: tsx:set-profile"
    - path: "commands/tsx/update.md"
      provides: "/tsx:update command"
      contains: "name: tsx:update"
    - path: "commands/tsx/validate-phase.md"
      provides: "/tsx:validate-phase command"
      contains: "name: tsx:validate-phase"
    - path: "commands/tsx/health.md"
      provides: "/tsx:health command"
      contains: "name: tsx:health"
    - path: "commands/tsx/cleanup.md"
      provides: "/tsx:cleanup command"
      contains: "name: tsx:cleanup"
    - path: "commands/tsx/add-tests.md"
      provides: "/tsx:add-tests command"
      contains: "argument-instructions"
  key_links:
    - from: "commands/tsx/map-codebase.md"
      to: "topstepx/workflows/map-codebase.md"
      via: "execution_context @ reference"
      pattern: "topstepx/workflows/map-codebase.md"
    - from: "commands/tsx/quick.md"
      to: "topstepx/workflows/quick.md"
      via: "execution_context @ reference"
      pattern: "topstepx/workflows/quick.md"
---

<objective>
Create 11 utility slash commands: map-codebase, quick, add-todo, check-todos, settings, set-profile, update, validate-phase, health, cleanup, add-tests.

Purpose: Complete the utility command surface. All are thin delegation commands adapted from GSD sources with systematic naming replacement.
Output: 11 Markdown command files in commands/tsx/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extended-commands/09-RESEARCH.md

Reference GSD source commands for exact content to adapt:
- ~/.claude/commands/gsd/map-codebase.md (71 lines, medium -- has when_to_use and success_criteria sections)
- ~/.claude/commands/gsd/quick.md (45 lines)
- ~/.claude/commands/gsd/add-todo.md (47 lines)
- ~/.claude/commands/gsd/check-todos.md (45 lines)
- ~/.claude/commands/gsd/settings.md (36 lines)
- ~/.claude/commands/gsd/set-profile.md (34 lines)
- ~/.claude/commands/gsd/update.md (37 lines)
- ~/.claude/commands/gsd/validate-phase.md (35 lines)
- ~/.claude/commands/gsd/health.md (22 lines)
- ~/.claude/commands/gsd/cleanup.md (18 lines -- no allowed-tools, no argument-hint)
- ~/.claude/commands/gsd/add-tests.md (41 lines -- has argument-instructions field)

Naming replacement table:
| GSD | TSX |
|-----|-----|
| `gsd:` in name: field | `tsx:` |
| `get-shit-done` in paths | `topstepx` |
| `gsd-planner` | `tsx-planner` |
| `gsd-executor` | `tsx-executor` |
| `gsd-codebase-mapper` | `tsx-codebase-mapper` |
| `/gsd:new-project` | `/tsx:new-project` |
| `/gsd:plan-phase` | `/tsx:plan-phase` |
| `GSD` in descriptions | `TSX` |
| `~/.gsd/` config paths | `~/.tsx/` |
| `/gsd:add-tests` in examples | `/tsx:add-tests` |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 simple utility commands (add-todo, check-todos, settings, set-profile, update, cleanup)</name>
  <files>commands/tsx/add-todo.md, commands/tsx/check-todos.md, commands/tsx/settings.md, commands/tsx/set-profile.md, commands/tsx/update.md, commands/tsx/cleanup.md</files>
  <action>
For each command, read the GSD source from ~/.claude/commands/gsd/ and create the TSX version with systematic replacements. These are the simplest commands (18-47 lines each).

1. **add-todo.md** (47 lines): Replace `gsd:add-todo` with `tsx:add-todo`. Replace "GSD session" with "TSX session" in objective text. Replace `get-shit-done` paths with `topstepx`. Keep allowed-tools (Read, Write, Bash, AskUserQuestion).

2. **check-todos.md** (45 lines): Replace `gsd:check-todos` with `tsx:check-todos`. Replace paths. Keep allowed-tools (Read, Write, Bash, AskUserQuestion).

3. **settings.md** (36 lines): Replace `gsd:settings` with `tsx:settings`. Replace description "Configure GSD workflow toggles" with "Configure TSX workflow toggles". Replace "Interactive configuration of GSD workflow agents" with "Interactive configuration of TSX workflow agents". Replace paths. Keep allowed-tools (Read, Write, Bash, AskUserQuestion).

4. **set-profile.md** (34 lines): Replace `gsd:set-profile` with `tsx:set-profile`. Replace description "Switch model profile for GSD agents" with "Switch model profile for TSX agents". Replace "used by GSD agents" with "used by TSX agents" in objective. Replace paths. Keep allowed-tools (Read, Write, Bash).

5. **update.md** (37 lines): Replace `gsd:update` with `tsx:update`. Replace description "Update GSD to latest version" with "Update TSX to latest version". Replace "Check for GSD updates" with "Check for TSX updates" in objective. Replace paths. Keep allowed-tools (Bash, AskUserQuestion).

6. **cleanup.md** (18 lines): Replace `gsd:cleanup` with `tsx:cleanup`. Replace paths. IMPORTANT: Do NOT add allowed-tools or argument-hint -- GSD intentionally omits both. This is the simplest command.

After creating all 6, verify zero GSD leaks:
```bash
grep -rn "gsd\|get-shit-done" commands/tsx/add-todo.md commands/tsx/check-todos.md commands/tsx/settings.md commands/tsx/set-profile.md commands/tsx/update.md commands/tsx/cleanup.md
```
  </action>
  <verify>
    <automated>bash -c "for f in add-todo check-todos settings set-profile update cleanup; do test -f commands/tsx/$f.md || echo \"MISSING: $f.md\"; done && grep -rn 'gsd\|get-shit-done' commands/tsx/add-todo.md commands/tsx/check-todos.md commands/tsx/settings.md commands/tsx/set-profile.md commands/tsx/update.md commands/tsx/cleanup.md; echo \"Exit: $?\"" | tail -5</automated>
  </verify>
  <done>6 simple utility commands exist in commands/tsx/ with zero GSD naming leaks; cleanup.md has no allowed-tools</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 medium utility commands (map-codebase, quick, validate-phase, health, add-tests)</name>
  <files>commands/tsx/map-codebase.md, commands/tsx/quick.md, commands/tsx/validate-phase.md, commands/tsx/health.md, commands/tsx/add-tests.md</files>
  <action>
For each command, read the GSD source from ~/.claude/commands/gsd/ and create the TSX version. These commands have slightly more content or special fields requiring attention.

1. **map-codebase.md** (71 lines, largest simple command): Replace `gsd:map-codebase` with `tsx:map-codebase`. Replace `gsd-codebase-mapper` with `tsx-codebase-mapper` in objective and process text. Replace `/gsd:new-project` with `/tsx:new-project` and `/gsd:plan-phase` with `/tsx:plan-phase` in context and process sections. Replace paths. Keep all sections: objective, execution_context, context, when_to_use, process, success_criteria. Keep allowed-tools (Read, Bash, Glob, Grep, Write, Task).

2. **quick.md** (45 lines): Replace `gsd:quick` with `tsx:quick`. Replace description "Execute a quick task with GSD guarantees" with "Execute a quick task with TSX guarantees". Replace `gsd-planner` with `tsx-planner` and `gsd-executor` with `tsx-executor` in objective text. Replace "GSD guarantees" with "TSX guarantees" in objective. Replace paths. Keep allowed-tools (Read, Write, Edit, Glob, Grep, Bash, Task, AskUserQuestion).

3. **validate-phase.md** (35 lines): Replace `gsd:validate-phase` with `tsx:validate-phase`. Replace paths. Keep allowed-tools (Read, Write, Edit, Bash, Glob, Grep, Task, AskUserQuestion).

4. **health.md** (22 lines): Replace `gsd:health` with `tsx:health`. Replace paths. Keep allowed-tools (Read, Bash, Write, AskUserQuestion).

5. **add-tests.md** (41 lines, has unique field): Replace `gsd:add-tests` with `tsx:add-tests`. Replace paths. CRITICAL: Preserve the `argument-instructions` frontmatter field -- replace `/gsd:add-tests` with `/tsx:add-tests` in the examples within that field. Keep allowed-tools (Read, Write, Edit, Bash, Glob, Grep, Task, AskUserQuestion).

After creating all 5, verify zero GSD leaks and that add-tests has argument-instructions:
```bash
grep -rn "gsd\|get-shit-done" commands/tsx/map-codebase.md commands/tsx/quick.md commands/tsx/validate-phase.md commands/tsx/health.md commands/tsx/add-tests.md
grep "argument-instructions" commands/tsx/add-tests.md
```
  </action>
  <verify>
    <automated>bash -c "for f in map-codebase quick validate-phase health add-tests; do test -f commands/tsx/$f.md || echo \"MISSING: $f.md\"; done && grep -c 'argument-instructions' commands/tsx/add-tests.md && grep -rn 'gsd\|get-shit-done' commands/tsx/map-codebase.md commands/tsx/quick.md commands/tsx/validate-phase.md commands/tsx/health.md commands/tsx/add-tests.md; echo \"Exit: $?\"" | tail -5</automated>
  </verify>
  <done>5 medium utility commands exist in commands/tsx/ with zero GSD naming leaks; add-tests.md has argument-instructions field; map-codebase.md references tsx-codebase-mapper</done>
</task>

</tasks>

<verification>
All 11 commands created with correct structure:
```bash
# Count new files (should be 11)
ls commands/tsx/add-todo.md commands/tsx/check-todos.md commands/tsx/settings.md commands/tsx/set-profile.md commands/tsx/update.md commands/tsx/cleanup.md commands/tsx/map-codebase.md commands/tsx/quick.md commands/tsx/validate-phase.md commands/tsx/health.md commands/tsx/add-tests.md | wc -l

# Zero GSD leaks
grep -rn "gsd\|get-shit-done" commands/tsx/add-todo.md commands/tsx/check-todos.md commands/tsx/settings.md commands/tsx/set-profile.md commands/tsx/update.md commands/tsx/cleanup.md commands/tsx/map-codebase.md commands/tsx/quick.md commands/tsx/validate-phase.md commands/tsx/health.md commands/tsx/add-tests.md

# Special fields preserved
grep "argument-instructions" commands/tsx/add-tests.md
grep -c "allowed-tools" commands/tsx/cleanup.md  # Should be 0
grep "tsx-codebase-mapper" commands/tsx/map-codebase.md
grep "tsx-planner" commands/tsx/quick.md
```
</verification>

<success_criteria>
- 11 command files exist in commands/tsx/
- All have correct `name: tsx:*` in YAML frontmatter
- All reference `$HOME/.claude/topstepx/workflows/` paths
- Zero matches for `gsd` or `get-shit-done` in any of the 11 files
- add-tests.md has `argument-instructions` field with `/tsx:add-tests` examples
- cleanup.md has NO `allowed-tools` field
- map-codebase.md references `tsx-codebase-mapper` (not gsd-codebase-mapper)
- quick.md references `tsx-planner` and `tsx-executor` (not gsd-*)
- settings.md says "TSX workflow toggles" (not GSD)
- set-profile.md says "TSX agents" (not GSD agents)
- update.md says "TSX" (not GSD) in description
</success_criteria>

<output>
After completion, create `.planning/phases/09-extended-commands/09-02-SUMMARY.md`
</output>
