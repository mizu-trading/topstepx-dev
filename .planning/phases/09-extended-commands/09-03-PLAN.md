---
phase: 09-extended-commands
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - commands/tsx/debug.md
  - commands/tsx/research-phase.md
autonomous: true
requirements: [CMD-17, CMD-20, CMD-26]

must_haves:
  truths:
    - "debug.md exists with inline orchestration logic fully adapted from GSD"
    - "research-phase.md exists with inline orchestration logic fully adapted from GSD"
    - "Both rich commands use tsx-tools.cjs (not gsd-tools.cjs) in all bash snippets"
    - "Both rich commands reference tsx-debugger / tsx-phase-researcher agents (not gsd-*)"
    - "Zero GSD naming leaks across ALL 32 command files in commands/tsx/"
    - "Total command count is 32 (12 Phase 8 + 20 Phase 9)"
  artifacts:
    - path: "commands/tsx/debug.md"
      provides: "/tsx:debug command with inline bash orchestration"
      contains: "tsx-tools.cjs"
    - path: "commands/tsx/research-phase.md"
      provides: "/tsx:research-phase command with inline bash orchestration"
      contains: "tsx-phase-researcher"
  key_links:
    - from: "commands/tsx/debug.md"
      to: "topstepx/bin/tsx-tools.cjs"
      via: "inline bash node call"
      pattern: "tsx-tools.cjs"
    - from: "commands/tsx/debug.md"
      to: "tsx-debugger agent"
      via: "Task() subagent spawning"
      pattern: "tsx-debugger"
    - from: "commands/tsx/research-phase.md"
      to: "topstepx/bin/tsx-tools.cjs"
      via: "inline bash node call"
      pattern: "tsx-tools.cjs"
    - from: "commands/tsx/research-phase.md"
      to: "tsx-phase-researcher agent"
      via: "Task() subagent spawning"
      pattern: "tsx-phase-researcher"
---

<objective>
Create 2 rich utility commands (debug, research-phase) that contain inline orchestration logic, then validate ALL 32 commands across the entire commands/tsx/ directory.

Purpose: Complete the final 2 complex commands that require line-by-line adaptation of inline bash, agent spawning templates, and checkpoint handling. Final validation confirms the complete TSX command surface.
Output: 2 Markdown command files in commands/tsx/ + validation confirming 32-command surface
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extended-commands/09-RESEARCH.md

Reference GSD source commands for exact content to adapt:
- ~/.claude/commands/gsd/debug.md (168 lines -- rich command with inline bash, agent spawning, checkpoints)
- ~/.claude/commands/gsd/research-phase.md (190 lines -- rich command with inline bash, agent spawning, continuation agents)

Naming replacement table (comprehensive for these two files):
| GSD Pattern | TSX Replacement | Occurrences |
|-------------|----------------|-------------|
| `gsd:debug` / `gsd:research-phase` | `tsx:debug` / `tsx:research-phase` | 1 each (name field) |
| `gsd-tools.cjs` | `tsx-tools.cjs` | 2 in debug, 3 in research-phase |
| `get-shit-done` (in paths) | `topstepx` | 2 in debug, 3+ in research-phase |
| `gsd-debugger` | `tsx-debugger` | 4 in debug |
| `gsd-phase-researcher` | `tsx-phase-researcher` | 4+ in research-phase |
| `/gsd:plan-phase` | `/tsx:plan-phase` | 1 in each |
| `GSD` (descriptive text) | `TSX` | multiple in both |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 2 rich commands with inline orchestration logic</name>
  <files>commands/tsx/debug.md, commands/tsx/research-phase.md</files>
  <action>
These 2 commands require careful line-by-line adaptation because they contain inline bash snippets, agent spawning templates, and checkpoint handling. Read each GSD source completely and adapt systematically.

1. **debug.md** (168 lines): Read ~/.claude/commands/gsd/debug.md in full. Create commands/tsx/debug.md with these replacements:
   - Frontmatter: `name: tsx:debug`, `description: Systematic debugging with persistent state across context resets`. Keep allowed-tools (Read, Bash, Task, AskUserQuestion).
   - objective: Replace "gsd-debugger" with "tsx-debugger" in orchestrator role text.
   - process Step 0: Replace `gsd-tools.cjs` with `tsx-tools.cjs` AND `get-shit-done` with `topstepx` in BOTH bash blocks (state load and resolve-model). Replace `gsd-debugger` with `tsx-debugger` in resolve-model call.
   - process Step 3: Replace `gsd-debugger` with `tsx-debugger` in the agent prompt heading AND in the Task() call's subagent_type.
   - process Step 4: Replace `/gsd:plan-phase --gaps` with `/tsx:plan-phase --gaps`.
   - process Step 5: Replace `gsd-debugger` with `tsx-debugger` in the continuation Task() call's subagent_type.
   - success_criteria: Replace `gsd-debugger` with `tsx-debugger`.

   After creation, verify all GSD references are replaced:
   ```bash
   grep -n "gsd\|get-shit-done" commands/tsx/debug.md
   ```
   Expected: ZERO matches.

2. **research-phase.md** (190 lines): Read ~/.claude/commands/gsd/research-phase.md in full. Create commands/tsx/research-phase.md with these replacements:
   - Frontmatter: `name: tsx:research-phase`, `description: Research how to implement a phase (standalone - usually use /tsx:plan-phase instead)`. Keep allowed-tools (Read, Bash, Task).
   - objective: Replace "gsd-phase-researcher" with "tsx-phase-researcher". Replace `/gsd:plan-phase` with `/tsx:plan-phase` in the note.
   - process Step 0: Replace `gsd-tools.cjs` with `tsx-tools.cjs` AND `get-shit-done` with `topstepx` in ALL bash blocks (init phase-op, resolve-model, roadmap get-phase). Replace `gsd-phase-researcher` with `tsx-phase-researcher` in resolve-model call.
   - process Step 1: Replace `gsd-tools.cjs` with `tsx-tools.cjs` AND `get-shit-done` with `topstepx` in the roadmap get-phase bash block.
   - process Step 4: Replace `gsd-phase-researcher` with `tsx-phase-researcher` in the agent prompt. Replace `/gsd:plan-phase` with `/tsx:plan-phase` in downstream_consumer text. Replace `gsd-phase-researcher` with `tsx-phase-researcher` in the Task() call.
   - process Step 6: Replace `gsd-phase-researcher` with `tsx-phase-researcher` in the continuation Task() call.
   - success_criteria: Replace `gsd-phase-researcher` with `tsx-phase-researcher`.

   After creation, verify all GSD references are replaced:
   ```bash
   grep -n "gsd\|get-shit-done" commands/tsx/research-phase.md
   ```
   Expected: ZERO matches.
  </action>
  <verify>
    <automated>bash -c "test -f commands/tsx/debug.md && test -f commands/tsx/research-phase.md && grep -c 'tsx-tools.cjs' commands/tsx/debug.md && grep -c 'tsx-debugger' commands/tsx/debug.md && grep -c 'tsx-tools.cjs' commands/tsx/research-phase.md && grep -c 'tsx-phase-researcher' commands/tsx/research-phase.md && echo '--- GSD leak check ---' && grep -rn 'gsd\|get-shit-done' commands/tsx/debug.md commands/tsx/research-phase.md; echo \"Exit: $?\""</automated>
  </verify>
  <done>Both rich commands exist with all inline bash adapted (tsx-tools.cjs, tsx-debugger, tsx-phase-researcher), zero GSD naming leaks, and correct agent spawning patterns</done>
</task>

<task type="auto">
  <name>Task 2: Validate complete 32-command TSX surface</name>
  <files></files>
  <action>
Run comprehensive validation across ALL 32 command files in commands/tsx/ (12 from Phase 8 + 20 from Phase 9):

1. **File count check:**
   ```bash
   ls commands/tsx/*.md | wc -l
   ```
   Expected: 32

2. **Zero GSD leaks across entire directory:**
   ```bash
   grep -rn "gsd\|get-shit-done" commands/tsx/
   ```
   Expected: ZERO matches

3. **All 20 Phase 9 commands have correct name: field:**
   ```bash
   for cmd in add-phase insert-phase remove-phase list-phase-assumptions plan-milestone-gaps map-codebase quick debug add-todo check-todos settings set-profile update research-phase validate-phase health cleanup add-tests adapt-language adapt-pinescript; do
     grep -q "name: tsx:$cmd" "commands/tsx/$cmd.md" && echo "OK: $cmd" || echo "FAIL: $cmd"
   done
   ```

4. **All workflow references resolve to existing files:**
   ```bash
   grep -h "topstepx/workflows/" commands/tsx/*.md | grep -oP 'topstepx/workflows/\S+\.md' | sort -u | while read wf; do
     test -f "$wf" && echo "EXISTS: $wf" || echo "MISSING: $wf"
   done
   ```

5. **Special field checks:**
   ```bash
   # add-tests has argument-instructions
   grep -c "argument-instructions" commands/tsx/add-tests.md

   # cleanup has no allowed-tools
   grep -c "allowed-tools" commands/tsx/cleanup.md

   # debug uses tsx-debugger (not gsd-debugger)
   grep -c "tsx-debugger" commands/tsx/debug.md

   # research-phase uses tsx-phase-researcher
   grep -c "tsx-phase-researcher" commands/tsx/research-phase.md

   # map-codebase uses tsx-codebase-mapper
   grep -c "tsx-codebase-mapper" commands/tsx/map-codebase.md
   ```

If any validation fails, fix the issue before completing. Report all validation results in the summary.
  </action>
  <verify>
    <automated>bash -c "COUNT=$(ls commands/tsx/*.md 2>/dev/null | wc -l) && LEAKS=$(grep -rn 'gsd\|get-shit-done' commands/tsx/ 2>/dev/null | wc -l) && echo \"Files: $COUNT (expect 32), GSD leaks: $LEAKS (expect 0)\" && test $COUNT -eq 32 && test $LEAKS -eq 0 && echo PASS || echo FAIL"</automated>
  </verify>
  <done>All 32 TSX commands validated: correct file count, zero GSD leaks, all workflow references valid, all special fields preserved</done>
</task>

</tasks>

<verification>
Complete Phase 9 validation:
```bash
# Total command count
ls commands/tsx/*.md | wc -l  # Must be 32

# Zero GSD leaks across entire directory
grep -rn "gsd\|get-shit-done" commands/tsx/  # Must be 0 matches

# Rich commands have correct inline references
grep "tsx-tools.cjs" commands/tsx/debug.md commands/tsx/research-phase.md
grep "tsx-debugger" commands/tsx/debug.md
grep "tsx-phase-researcher" commands/tsx/research-phase.md
```
</verification>

<success_criteria>
- debug.md exists (~168 lines) with ALL inline bash using tsx-tools.cjs and tsx-debugger
- research-phase.md exists (~190 lines) with ALL inline bash using tsx-tools.cjs and tsx-phase-researcher
- Total commands/tsx/ count is exactly 32
- Zero matches for `gsd` or `get-shit-done` across ALL 32 files
- All workflow @ references point to existing files in topstepx/workflows/
- All special fields preserved (argument-instructions on add-tests, no allowed-tools on cleanup)
</success_criteria>

<output>
After completion, create `.planning/phases/09-extended-commands/09-03-SUMMARY.md`
</output>
