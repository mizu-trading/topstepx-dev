---
phase: 09-extended-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/tsx/add-phase.md
  - commands/tsx/insert-phase.md
  - commands/tsx/remove-phase.md
  - commands/tsx/list-phase-assumptions.md
  - commands/tsx/plan-milestone-gaps.md
  - commands/tsx/adapt-language.md
  - commands/tsx/adapt-pinescript.md
autonomous: true
requirements: [CMD-13, CMD-14, CMD-15, CMD-16, CMD-17, CMD-31, CMD-32]

must_haves:
  truths:
    - "All 5 phase management commands exist as /tsx:* slash commands in commands/tsx/"
    - "Both TSX-specific commands exist and delegate to their respective workflows"
    - "Zero GSD naming leaks across all 7 command files"
    - "All 7 commands reference correct topstepx/workflows/ files"
  artifacts:
    - path: "commands/tsx/add-phase.md"
      provides: "/tsx:add-phase command"
      contains: "name: tsx:add-phase"
    - path: "commands/tsx/insert-phase.md"
      provides: "/tsx:insert-phase command"
      contains: "name: tsx:insert-phase"
    - path: "commands/tsx/remove-phase.md"
      provides: "/tsx:remove-phase command"
      contains: "name: tsx:remove-phase"
    - path: "commands/tsx/list-phase-assumptions.md"
      provides: "/tsx:list-phase-assumptions command"
      contains: "name: tsx:list-phase-assumptions"
    - path: "commands/tsx/plan-milestone-gaps.md"
      provides: "/tsx:plan-milestone-gaps command"
      contains: "name: tsx:plan-milestone-gaps"
    - path: "commands/tsx/adapt-language.md"
      provides: "/tsx:adapt-language command"
      contains: "name: tsx:adapt-language"
    - path: "commands/tsx/adapt-pinescript.md"
      provides: "/tsx:adapt-pinescript command"
      contains: "name: tsx:adapt-pinescript"
  key_links:
    - from: "commands/tsx/add-phase.md"
      to: "topstepx/workflows/add-phase.md"
      via: "execution_context @ reference"
      pattern: "topstepx/workflows/add-phase.md"
    - from: "commands/tsx/adapt-language.md"
      to: "topstepx/workflows/adapt-language.md"
      via: "execution_context @ reference"
      pattern: "topstepx/workflows/adapt-language.md"
    - from: "commands/tsx/adapt-pinescript.md"
      to: "topstepx/workflows/adapt-pinescript.md"
      via: "execution_context @ reference"
      pattern: "topstepx/workflows/adapt-pinescript.md"
---

<objective>
Create 7 slash commands: 5 phase management commands (add-phase, insert-phase, remove-phase, list-phase-assumptions, plan-milestone-gaps) and 2 TSX-specific commands (adapt-language, adapt-pinescript).

Purpose: Complete the phase management and TSX-specific command surface. All are thin delegation commands following the established Phase 8 pattern.
Output: 7 Markdown command files in commands/tsx/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extended-commands/09-RESEARCH.md

Reference the GSD source commands for exact content to adapt:
- ~/.claude/commands/gsd/add-phase.md (43 lines)
- ~/.claude/commands/gsd/insert-phase.md (32 lines)
- ~/.claude/commands/gsd/remove-phase.md (31 lines)
- ~/.claude/commands/gsd/list-phase-assumptions.md (46 lines)
- ~/.claude/commands/gsd/plan-milestone-gaps.md (34 lines)

For TSX-specific commands, use the Phase 8 command pattern (e.g., commands/tsx/execute-phase.md) as structural template.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 phase management commands</name>
  <files>commands/tsx/add-phase.md, commands/tsx/insert-phase.md, commands/tsx/remove-phase.md, commands/tsx/list-phase-assumptions.md, commands/tsx/plan-milestone-gaps.md</files>
  <action>
For each of the 5 phase management commands, read the corresponding GSD source file from ~/.claude/commands/gsd/ and create the TSX version in commands/tsx/ with these systematic replacements:

1. **add-phase.md** (from GSD 43 lines): Replace `name: gsd:add-phase` with `name: tsx:add-phase`. Replace `get-shit-done` path segments with `topstepx`. Use `@$HOME/.claude/topstepx/workflows/add-phase.md` for execution_context and process references. Keep all content structure identical.

2. **insert-phase.md** (from GSD 32 lines): Replace `name: gsd:insert-phase` with `name: tsx:insert-phase`. Replace paths from `get-shit-done` to `topstepx`. Keep argument-hint `<after> <description>` and all allowed-tools (Read, Write, Bash).

3. **remove-phase.md** (from GSD 31 lines): Replace `name: gsd:remove-phase` with `name: tsx:remove-phase`. Replace paths. Keep allowed-tools including Glob. Keep argument-hint `<phase-number>`.

4. **list-phase-assumptions.md** (from GSD 46 lines): Replace `name: gsd:list-phase-assumptions` with `name: tsx:list-phase-assumptions`. Replace paths. Keep success_criteria section. Keep allowed-tools (Read, Bash, Grep, Glob).

5. **plan-milestone-gaps.md** (from GSD 34 lines): Replace `name: gsd:plan-milestone-gaps` with `name: tsx:plan-milestone-gaps`. Replace paths. CRITICAL: Replace `/gsd:audit-milestone` with `/tsx:audit-milestone` and `/gsd:add-phase` with `/tsx:add-phase` in body text. Keep allowed-tools (Read, Write, Bash, Glob, Grep, AskUserQuestion).

After creating all 5 files, verify zero GSD leaks:
```bash
grep -rn "gsd\|get-shit-done" commands/tsx/add-phase.md commands/tsx/insert-phase.md commands/tsx/remove-phase.md commands/tsx/list-phase-assumptions.md commands/tsx/plan-milestone-gaps.md
```
  </action>
  <verify>
    <automated>bash -c "grep -rn 'gsd\|get-shit-done' commands/tsx/add-phase.md commands/tsx/insert-phase.md commands/tsx/remove-phase.md commands/tsx/list-phase-assumptions.md commands/tsx/plan-milestone-gaps.md; echo \"Exit: $?\"" | tail -5</automated>
  </verify>
  <done>5 phase management command files exist in commands/tsx/ with correct tsx: naming, topstepx paths, and zero GSD naming leaks</done>
</task>

<task type="auto">
  <name>Task 2: Create 2 TSX-specific commands</name>
  <files>commands/tsx/adapt-language.md, commands/tsx/adapt-pinescript.md</files>
  <action>
These 2 commands have no GSD equivalent -- author them fresh following the thin-delegation pattern established in Phase 8. Use commands/tsx/execute-phase.md as the structural template.

1. **adapt-language.md** (CMD-31, ~30 lines):
   - Frontmatter: `name: tsx:adapt-language`, `description: Convert TopStepX trading bot code between supported languages`, `argument-hint: "<source-path> --to <target-language>"`, allowed-tools: Read, Write, Bash, Glob, Grep, Task, AskUserQuestion
   - objective: Convert an existing TopStepX trading bot from one supported language to another while preserving all trading logic, API integration patterns, and safety guardrails. Routes to the adapt-language workflow which handles source code analysis, library mapping, code generation following trading build order, and safety verification gate.
   - execution_context: `@$HOME/.claude/topstepx/workflows/adapt-language.md`
   - context: `Arguments: $ARGUMENTS (source path and target language)`
   - process: Execute the adapt-language workflow end-to-end. Preserve all workflow gates (source analysis, library mapping, code generation, safety verification).

2. **adapt-pinescript.md** (CMD-32, ~35 lines):
   - Frontmatter: `name: tsx:adapt-pinescript`, `description: Convert TradingView PineScript strategy to a live-tradeable TopStepX trading bot`, `argument-hint: "<pinescript-source>"`, allowed-tools: Read, Write, Bash, Glob, Grep, Task, AskUserQuestion
   - objective: Convert a TradingView PineScript strategy into a working, live-tradeable TopStepX bot with all safety guardrails. Routes to the adapt-pinescript workflow which handles PineScript parsing, repainting audit, MTF audit, conversion mapping, code generation, and SAF-01 through SAF-05 safety verification.
   - execution_context: `@$HOME/.claude/topstepx/workflows/adapt-pinescript.md`
   - context: `Arguments: $ARGUMENTS (path to PineScript source file)`
   - process: Execute the adapt-pinescript workflow end-to-end. Preserve all workflow gates (PineScript analysis, repainting audit, MTF audit, signal confirmation, code generation, safety verification).

After creating both files, verify they exist and reference correct workflows:
```bash
grep "topstepx/workflows/adapt-language.md" commands/tsx/adapt-language.md
grep "topstepx/workflows/adapt-pinescript.md" commands/tsx/adapt-pinescript.md
```
  </action>
  <verify>
    <automated>bash -c "test -f commands/tsx/adapt-language.md && test -f commands/tsx/adapt-pinescript.md && grep -q 'tsx:adapt-language' commands/tsx/adapt-language.md && grep -q 'tsx:adapt-pinescript' commands/tsx/adapt-pinescript.md && echo PASS || echo FAIL"</automated>
  </verify>
  <done>Both TSX-specific commands exist with correct frontmatter, workflow delegation, and Task in allowed-tools for agent spawning</done>
</task>

</tasks>

<verification>
All 7 commands created with correct structure:
```bash
# Count new files (should be 7)
ls commands/tsx/add-phase.md commands/tsx/insert-phase.md commands/tsx/remove-phase.md commands/tsx/list-phase-assumptions.md commands/tsx/plan-milestone-gaps.md commands/tsx/adapt-language.md commands/tsx/adapt-pinescript.md | wc -l

# Zero GSD leaks across all 7 files
grep -rn "gsd\|get-shit-done" commands/tsx/add-phase.md commands/tsx/insert-phase.md commands/tsx/remove-phase.md commands/tsx/list-phase-assumptions.md commands/tsx/plan-milestone-gaps.md commands/tsx/adapt-language.md commands/tsx/adapt-pinescript.md

# All reference correct workflows
grep "topstepx/workflows/" commands/tsx/add-phase.md commands/tsx/insert-phase.md commands/tsx/remove-phase.md commands/tsx/list-phase-assumptions.md commands/tsx/plan-milestone-gaps.md commands/tsx/adapt-language.md commands/tsx/adapt-pinescript.md
```
</verification>

<success_criteria>
- 7 command files exist in commands/tsx/
- All have correct `name: tsx:*` in YAML frontmatter
- All reference `$HOME/.claude/topstepx/workflows/` paths
- Zero matches for `gsd` or `get-shit-done` in any of the 7 files
- plan-milestone-gaps references /tsx:audit-milestone (not /gsd:audit-milestone)
- adapt-language and adapt-pinescript include Task in allowed-tools
</success_criteria>

<output>
After completion, create `.planning/phases/09-extended-commands/09-01-SUMMARY.md`
</output>
